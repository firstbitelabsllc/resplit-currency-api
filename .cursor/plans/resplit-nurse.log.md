# Resplit Nurse Log

## 2026-05-25 05:08 EDT

- `GO/exact-source-promotion-bundle-staged`, `GO/staged-index-attestation`, `GO/cockpit-staged-attestation-ui`, `NO-GO/source-promotion-landed`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: staged only the 20-path source-promotion bundle generated by `reports/resplit-fx-source-promotion-packet.json`. Added a staged-bundle attestation layer to `scripts/source-promotion-packet.js` and `scripts/reliability-cockpit.js` so the packet and GUI distinguish "stageable" from "actually staged exactly." The attestation fails red on unexpected staged paths, hold-by-default staged paths, or files changed again after staging.
- Current source-promotion truth: `stagedBundle.status=green`, `exactMatch=true`, `stageableCount=20`, `stagedStageableCount=20`, `unstagedStageableCount=0`, `unexpectedStagedCount=0`, and `dirtyAfterStagingCount=0`. The packet remains overall red because the bundle is staged but not committed/landed; clean FirstBite proof remains blocked until this exact bundle is landed or otherwise run from a clean tracked source state.
- Branch/PR promotion: moved the exact reviewed bundle onto clean worktree `/Users/leokwan/Development/resplit-currency-api-worktrees/source-promotion-attested-20260525` from `origin/main`, resolved three upstream-overlap conflicts by preserving the reviewed local-CI/smoke hardening, amended CodeQL escaping/URL-sanitization fixes into commit `bb842d3 vidux: promote FX reliability cockpit source`, pushed branch `codex/resplit-fx-source-promotion-attested-20260525`, and opened ready PR #7 (`https://github.com/firstbitelabsllc/resplit-currency-api/pull/7`). In that clean branch, the staged source-promotion delta is 18 paths because `origin/main` already contains the audit-history script/test pair.
- Fresh proof:
  - `git diff --cached --check` -> pass.
  - `git diff --cached --name-status` -> exactly the 20 source-promotion candidate paths, no hold-by-default rows.
  - `npm run source:promotion-packet` -> expected rc `1`; `stagedBundle.status=green`, `exactMatch=true`, `20/20` staged, zero unexpected staged paths, zero dirty-after-staging paths.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js` -> 50/50.
  - `npm test` -> 197/197.
  - `npm run trust:preflight` -> expected rc `1`; status red with commands `7 green, 2 yellow, 0 red`; cockpit remains `RED - missing required trust contract`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Staged bundle attestation`, `Exact source-promotion bundle is staged (20/20)`, `Verify staged exact bundle`, `Unexpected staged`, `Dirty after staging`, and `RED - missing required trust contract`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-staged-attestation.png` and `reports/resplit-fx-reliability-cockpit-mobile-staged-attestation.png`; both reported no horizontal overflow.
  - Post-CodeQL-fix primary mirror: `git diff --cached --check` -> pass; `node --test tests/source-promotion-packet.test.js tests/trust-preflight.test.js tests/verify-grafana-otel-smoke.test.js` -> 19/19; `npm run source:promotion-packet` -> expected rc `1` with `stagedBundle.status=green`, `exactMatch=true`, and `20/20` source-promotion paths staged.
  - Clean branch proof: `npm ci`; `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js tests/smoke-check-deploy.test.js` -> 70/70 before the CodeQL patch; `node --test tests/source-promotion-packet.test.js tests/trust-preflight.test.js tests/verify-grafana-otel-smoke.test.js` -> 19/19 after the CodeQL patch; `npm run generate`; `npm test` -> 209/209; `npm run check` -> pass; `npm run smoke:deploy` -> pass with `smoke-check-deploy: OK (date=2026-05-25, historyPoints=30, cf=https://resplit-currency-api.pages.dev)`.
  - PR check truth at last poll: CodeQL `Analyze (actions)`, CodeQL `Analyze (javascript-typescript)`, aggregate `CodeQL`, and Graphite mergeability all succeeded; Graphite AI review was skipped; GitHub reports merge state `CLEAN`.
- Current blocker: source-promotion is now index-attested, PR-open, and GitHub-check-clean, but not landed. The primary checkout is still behind `origin/main` by 10 and carries unrelated hold-by-default dirt outside the staged bundle; loaded Codex/Cursor MCP still exposes the stale 12-lane catalog without `resplit_currency_api`; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; and clean FirstBite proof still needs to run from the landed tracked source.
- Exact next slice: merge PR #7, then run `worktree=true` FirstBite proof from the landed tracked source. Keep loaded MCP restart, M4 peer execution, Grafana proof, and release-history as separate trust gates.

## 2026-05-25 04:57 EDT

- `GO/hash-locked-reviewed-drift`, `GO/full-stage-gate-unblocked`, `GO/smoke-health-guard-preserved`, `GO/cockpit-reviewed-decision-ui`, `NO-GO/source-promotion-landed`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: added `.firstbite/source-promotion-decisions.json`, a hash-locked review manifest for the four remaining origin-drift rows. `scripts/source-promotion-packet.js` now loads that manifest and only converts a red origin-drift row to yellow when `decision=accept-current` and both the current/origin hashes still match the reviewed blobs. `scripts/reliability-cockpit.js` includes the decision manifest in the source-promotion bundle and renders review-decision status in the `Source Promotion Packet Reconciliation` table.
- Safety merge finding: current `scripts/smoke-check-deploy.js` had publish-grace/recovery-gap hardening but had dropped upstream Worker `/health` release/environment checks. This pass restored the health check and `isRecoveryCoverageGap` provenance guard while preserving publish grace, GitHub fallback latest-date checking, and smoke tests for those paths. `package.json` also preserves origin/main `backfill:history` while keeping the new cockpit/preflight/local-CI scripts.
- Current source-promotion truth: packet remains red because the source-promotion bundle is not landed and clean proof has not run, but the staging gate is now yellow/unblocked: `stageCandidates=20`, `stageBlockedPaths=0`, `stageNonRedPaths=20`, `reconciliationRed=0`, `reconciliationYellow=20`, `dirtyFiles=44`, decision count `4`. `stageExactBundle` is executable and includes `.firstbite/source-promotion-decisions.json`; hold-by-default rows remain out of the bundle.
- Fresh proof:
  - `node --check scripts/source-promotion-packet.js && node --check scripts/reliability-cockpit.js && node --check scripts/smoke-check-deploy.js` -> pass.
  - `node --test tests/smoke-check-deploy.test.js` -> 16/16.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js tests/smoke-check-deploy.test.js` -> 65/65.
  - `npm run source:promotion-packet` -> expected rc `1`; status red; staging gate yellow with no blocked paths, `reviewDecisions.count=4`, and full stage command generated.
  - `npm test` -> 196/196.
  - `npm run trust:preflight` -> expected rc `1`; status red with commands `7 green, 2 yellow, 0 red`; cockpit still `RED - missing required trust contract`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Full bundle staging is available after reviewing 20 non-red candidate`, `No red rows block full-bundle staging`, `accepted:accept-current`, `origin-present-origin-drift-reviewed`, `modified-tracked-origin-drift-reviewed`, `.firstbite/source-promotion-decisions.json`, and `RED - missing required trust contract`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-reviewed-drift.png` and `reports/resplit-fx-reliability-cockpit-mobile-reviewed-drift.png`; both reported no horizontal overflow.
- Current blocker: this makes the source-promotion bundle stageable with review evidence, but it is not staged/landed and therefore cannot be counted as clean tracked local CI. Clean FirstBite proof remains blocked until the exact bundle is landed and run from a clean worktree; loaded Codex/Cursor MCP host remains stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: review the generated `stageExactBundle`, stage only that exact source-promotion bundle when ready, run `git diff --cached --stat && git diff --cached --check`, then run clean `worktree=true` FirstBite proof. Keep loaded MCP, M4, Grafana, and release-history as separate trust gates.

## 2026-05-25 04:48 EDT

- `GO/audit-history-origin-reconciliation`, `GO/source-promotion-red-row-reduction`, `GO/cockpit-4-blocker-ui`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/audit-history-backfill-sources.js` and `tests/audit-history-backfill-sources.test.js` now match `origin/main` exactly. This preserves upstream `fxapi-pair-history`, explicit deterministic derivations (`fok<-dkk`, `kid<-aud`, `tvd<-aud`), derivation provenance in reports, stricter impossible-date validation, and the upstream coverage tests instead of letting the local bundle delete them.
- Current source-promotion truth: packet remains red, but the hard staging gate improved from `6` blocked red paths to `4`. `stageExactBundle` is still `BLOCKED: resolve 4 red candidate(s) before staging the full bundle`; `stageNonRedCandidates` now includes the reconciled audit-history script/test. Remaining red blocked paths are `.firstbite/local-ci.json`, `package.json`, `scripts/smoke-check-deploy.js`, and `tests/smoke-check-deploy.test.js`.
- Fresh proof:
  - `diff -u <(git show origin/main:scripts/audit-history-backfill-sources.js) scripts/audit-history-backfill-sources.js` -> no diff.
  - `diff -u <(git show origin/main:tests/audit-history-backfill-sources.test.js) tests/audit-history-backfill-sources.test.js` -> no diff.
  - `node --check scripts/audit-history-backfill-sources.js` -> pass.
  - `node --test tests/audit-history-backfill-sources.test.js` -> 10/10.
  - `npm run source:promotion-packet` -> expected rc `1`; status red; `stageBlockedPaths=4`, `stageNonRedPaths=15`, `reconciliationRed=4`, `reconciliationYellow=15`, `dirtyFiles=43`.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js tests/audit-history-backfill-sources.test.js` -> 58/58.
  - `npm run trust:preflight` -> expected rc `1`; status red with commands `7 green, 2 yellow, 0 red`; cockpit still `RED - missing required trust contract`.
  - `npm test` -> 192/192.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Full stage gate`, `BLOCKED: resolve 4 red candidate`, `Stage non-red candidates`, the three remaining smoke/manifest blockers, and the reconciled audit-history path; screenshots `reports/resplit-fx-reliability-cockpit-desktop-staging-gate.png` and `reports/resplit-fx-reliability-cockpit-mobile-staging-gate.png`; both reported no horizontal overflow.
- Current blocker: this reconciles one dangerous upstream-drift pair, but source promotion is still not landed. Clean tracked FirstBite proof remains blocked until the four remaining red paths are reconciled and the intended bundle lands; loaded Codex/Cursor MCP host remains stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: reconcile `.firstbite/local-ci.json`, `package.json`, `scripts/smoke-check-deploy.js`, and `tests/smoke-check-deploy.test.js` against `origin/main`, then regenerate the packet until `stageExactBundle` becomes executable. Keep clean FirstBite, loaded MCP, M4, Grafana, and release-history as separate gates.

## 2026-05-25 04:43 EDT

- `GO/source-promotion-staging-gate`, `GO/cockpit-blocked-stage-ui`, `GO/red-row-diff-inspection`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/source-promotion-packet.js` now builds a `stagingGate` from candidate reconciliation. If any red upstream-drift row exists, `commands.stageExactBundle` becomes a non-executable `BLOCKED: resolve ...` guard, `commands.stageNonRedCandidates` exposes only the non-red reviewed subset, and the packet records blocked paths/commands. `scripts/reliability-cockpit.js` renders `Full stage gate`, blocked paths, red-row review commands, and the non-red stage command directly in `Source Promotion Packet Reconciliation`.
- Red-row inspection finding: after `git fetch --prune`, the six red rows still exist. The dangerous one is not theoretical: staging the current local `scripts/audit-history-backfill-sources.js` and `tests/audit-history-backfill-sources.test.js` as-is would delete upstream `origin/main` fxapi pair-history and deterministic derivation coverage (`-180` and `-120` line deltas). Full staging is therefore correctly blocked until those rows are reconciled with origin.
- Current source-promotion truth: packet remains red with `19` candidates, `6` blocked red paths, `13` non-red stageable paths, `24` hold-by-default rows, and `43` dirty rows. `stageExactBundle` is now `BLOCKED: resolve 6 red candidate(s) before staging the full bundle`; `stageNonRedCandidates` excludes `.firstbite/local-ci.json`, `package.json`, `scripts/smoke-check-deploy.js`, `scripts/audit-history-backfill-sources.js`, `tests/audit-history-backfill-sources.test.js`, and `tests/smoke-check-deploy.test.js`.
- Current loaded-MCP truth: live `mcp__firstbite_local_ci__.list_lanes` still exposes the stale 12-lane loaded-host catalog with no `resplit_currency_api`; repo-backed MCP source remains the only current 15-lane claim.
- Fresh proof:
  - `git fetch --prune` -> refreshed remotes; `origin/gh-pages` forced update noted; `main` still behind `origin/main` by `10`.
  - Red-row diff inspection -> `.firstbite/local-ci.json` command drift only; `package.json` adds trust/cockpit scripts; `scripts/smoke-check-deploy.js` adds publish-grace/recovery-gap smoke hardening; audit-history current local copies would remove upstream fxapi/derivation logic; `tests/smoke-check-deploy.test.js` overlaps branch drift and local edits.
  - `node --check scripts/source-promotion-packet.js` / `scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js` -> 48/48.
  - `npm test` -> 187/187.
  - `npm run source:promotion-packet` -> expected rc `1`; status red; `stageBlockedPaths=6`, `stageNonRedPaths=13`.
  - `npm run trust:preflight` -> expected rc `1`; status red with commands `7 green, 2 yellow, 0 red`; cockpit still `RED - missing required trust contract`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Full stage gate`, `BLOCKED: resolve 6 red candidate`, `Stage non-red candidates`, `scripts/audit-history-backfill-sources.js`, `origin-present-origin-drift`, and `modified-tracked-origin-drift`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-staging-gate.png` and `reports/resplit-fx-reliability-cockpit-mobile-staging-gate.png`; both reported no horizontal overflow.
- Current blocker: this prevents accidental destructive staging, but source promotion is still not landed. Clean tracked FirstBite proof remains blocked until the six red paths are reconciled and the intended bundle lands; loaded Codex/Cursor MCP host remains stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: reconcile the six blocked paths, starting with preserving/merging the upstream audit-history fxapi/derivation work instead of overwriting it, then regenerate the packet until `stageExactBundle` becomes executable. Only after that should anyone stage the full bundle and run clean `worktree=true` FirstBite proof.

## 2026-05-25 04:36 EDT

- `GO/source-promotion-review-hints`, `GO/cockpit-review-command-ui`, `GO/browser-review-hint-proof`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/source-promotion-packet.js` now adds per-candidate `currentLines`, `headLines`, `originLines`, `lineDeltaVsHead`, `lineDeltaVsOrigin`, and `reviewCommand` fields so every stage-candidate row has a direct read-only review path. `scripts/reliability-cockpit.js` now renders those line counts, deltas, and review commands in `Source Promotion Packet Reconciliation`, keeping packet and GUI review surfaces aligned.
- Current source-promotion truth: packet remains red. Reconciliation still reviews `19` candidates with `3` already present on `origin/main`, `6` origin drift rows, `10` new local-only files, and `6` modified tracked files. The six red rows remain `.firstbite/local-ci.json`, `package.json`, `scripts/smoke-check-deploy.js`, `scripts/audit-history-backfill-sources.js`, `tests/audit-history-backfill-sources.test.js`, and `tests/smoke-check-deploy.test.js`; each now carries the exact review command and line delta. `git diff --no-index` review commands are intentionally diff commands, so nonzero means content differs, not that the proof command is broken.
- Fresh proof:
  - `node --check scripts/source-promotion-packet.js` / `scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js` -> 47/47.
  - `npm run source:promotion-packet` -> expected rc `1`; status red; review summary `19 candidate(s) reviewed; 3 already present on origin/main; 6 origin drift row(s); 10 new local-only file(s); 6 modified tracked file(s)`.
  - Generated red-row commands include fixed temp-file cleanup syntax: `rc=$?; rm -f "$tmp"; exit $rc`.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html`; cockpit remains `RED - missing required trust contract`.
  - `npm test` -> 186/186.
  - `npm run trust:preflight` -> expected rc `1`; status red with commands `7 green, 2 yellow, 0 red`; cockpit still red.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Source Promotion Packet Reconciliation`, `Review command`, `Delta origin`/`Δ origin`, `git diff --no-index`, `rc=$?; rm -f`, `git diff origin/main`, `modified-tracked-origin-drift`, and `origin-present-origin-drift`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-source-review-hints.png` and `reports/resplit-fx-reliability-cockpit-mobile-source-review-hints.png`; both reported no horizontal overflow.
- Current blocker: this makes the reconciliation rows actionable, but it does not land the source-promotion bundle. Clean tracked FirstBite proof remains blocked until the upstream-drift rows are reconciled and the intended bundle lands; loaded Codex/Cursor MCP host remains stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: execute or inspect the six red-row review commands, decide intended current-vs-origin content for each upstream-drift path, then stage/land only the reconciled bundle and rerun clean `worktree=true` FirstBite proof. If source promotion waits, restart/reload the loaded MCP host and capture live `list_lanes` separately.

## 2026-05-25 04:28 EDT

- `GO/source-promotion-reconciliation`, `GO/cockpit-reconciliation-ui`, `GO/proof-freshness-ledger`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/source-promotion-packet.js` now adds `promotionReview`, a candidate reconciliation layer that hashes every stage candidate against the working tree, `HEAD`, and `origin/main`. The packet now classifies rows as `new-local-only`, `modified-tracked`, `origin-present-match`, `origin-present-origin-drift`, or `modified-tracked-origin-drift`, adds `git diff origin/main -- ...`, and blocks blind staging when upstream already has different content. `scripts/reliability-cockpit.js` now reads the packet artifact and renders `Source Promotion Packet Reconciliation` inside the cockpit so the GUI shows the staging hazards, not just the stage command.
- Current source-promotion truth: packet remains red. Reconciliation reviewed `19` candidates: `3` already present on `origin/main`, `6` origin drift rows, `10` new local-only files, `6` modified tracked files. Red reconciliation rows include `.firstbite/local-ci.json`, `package.json`, `scripts/smoke-check-deploy.js`, `scripts/audit-history-backfill-sources.js`, `tests/audit-history-backfill-sources.test.js`, and `tests/smoke-check-deploy.test.js`; these must be diffed against `origin/main` before any staging/promotion claim.
- Current loaded-MCP truth: live `mcp__firstbite_local_ci__.list_lanes` still exposes the stale 12-lane loaded-host catalog with no `resplit_currency_api`; repo-backed MCP source remains the only green MCP catalog claim.
- Fresh proof:
  - `node --check scripts/source-promotion-packet.js` / `scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js` -> 47/47.
  - `npm run source:promotion-packet` -> expected rc `1`; status red; reconciliation red with `19` candidates, `6` red, `13` yellow, `3` upstream-present, `6` upstream-drift.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; cockpit remains `RED - missing required trust contract`; `localCi.sourcePromotionPacket` includes reconciliation red.
  - `npm test` -> 186/186.
  - `npm run trust:preflight` -> expected rc `1`; status red with commands `7 green, 2 yellow, 0 red`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Source Promotion Packet Reconciliation`, `origin-present-origin-drift`, `modified-tracked-origin-drift`, `git diff origin/main`, and the reconciliation action text; screenshots `reports/resplit-fx-reliability-cockpit-desktop-source-reconciliation.png` and `reports/resplit-fx-reliability-cockpit-mobile-source-reconciliation.png`; both reported no horizontal overflow.
- Current blocker: this makes source promotion safer but does not land it. Clean tracked FirstBite proof remains blocked until the upstream-drift rows are reconciled and the intended bundle lands; loaded Codex/Cursor MCP host remains stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: reconcile the six origin-drift candidate rows with `git diff origin/main -- <path>` before staging anything, then land only the intended source-promotion bundle and rerun clean `worktree=true` FirstBite proof. Keep the loaded MCP, M4, Grafana, and release-history gates separate.

## 2026-05-25 04:19 EDT

- `GO/launch-trust-audit`, `GO/operator-action-queue`, `GO/proof-freshness-ledger`, `GO/repo-backed-mcp-source`, `NO-GO/overall-launch-trust`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now builds and renders `trustModel.launchTrustAudit`, a launch-claim boundary layer with nine rows: `overall-launch-trust`, `source-contract`, `clean-firstbite-local-ci`, `loaded-agent-mcp`, `repo-backed-mcp-source`, `peer-execution`, `otel-grafana-proof`, `release-history-quality`, and `agent-ledger-fleet`. Each row states the strongest allowed claim, the forbidden overclaim, evidence path, current gap, and next action so operators cannot confuse freshness, support-only status, repo-backed source, or config presence with launch trust.
- Current launch-trust truth: audit status is red: `1 allowed, 4 caution, 4 forbidden claim boundary(s) across 9 launch-trust surfaces.` The only allowed claim is `repo-backed-mcp-source`, which sees `repo-manifest-v2` with 15 lane(s) and all 3/3 expected FX lanes in the repo-backed MCP package source. The overall launch claim remains forbidden while source contract, clean FirstBite, loaded-agent MCP, peer execution, OTEL/Grafana, release-history, and fleet warnings are not all green.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/reliability-cockpit.test.js` -> 41/41.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; cockpit remains `RED - missing required trust contract`; launch trust audit status red.
  - `npm run source:promotion-packet` -> expected rc `1`; artifact status red with `19` stage candidates, `24` hold-by-default dirty rows, and `6` command drift rows.
  - `npm run trust:preflight` -> expected rc `1`; cockpit remains red with commands `7 green, 2 yellow, 0 red`.
  - `npm test` -> 184/184.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Launch Trust Audit`, `overall-launch-trust`, `loaded-agent-mcp`, `repo-backed-mcp-source`, and `otel-grafana-proof`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-launch-trust-audit.png` and `reports/resplit-fx-reliability-cockpit-mobile-launch-trust-audit.png`; both reported no horizontal overflow.
- Current blocker: this improves overclaim resistance, not release readiness. Source promotion is still unlanded; clean tracked FirstBite proof is still blocked on that bundle; loaded Codex/Cursor MCP host remains stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 remains support-only until M4-local execute proof exists; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: either land the source-promotion bundle and rerun clean `worktree=true` FirstBite proof, or restart/reload the loaded MCP host and capture live `list_lanes`. Keep the Launch Trust Audit as the first-screen overclaim guard until every launch-critical row is green.

## 2026-05-25 04:11 EDT

- `GO/loaded-mcp-probe-command`, `GO/operator-action-queue`, `GO/proof-freshness-ledger`, `NO-GO/loaded-mcp-catalog`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/capture-loaded-mcp-probe.js` now supports `--reuse-existing`, which refreshes `reports/firstbite-loaded-mcp-lanes.json` from its previous payload without claiming host-restart proof. Empty plain stdin now fails with an actionable message naming live MCP JSON, `--input`, and `--reuse-existing`. `scripts/reliability-cockpit.js` now points the `loaded-mcp-refresh` operator action and freshness ledger at `npm run mcp:loaded-probe -- --reuse-existing`, while the action text still says true green requires a restarted/reloaded Codex/Cursor MCP host plus live `list_lanes` capture.
- Current loaded-MCP truth: live `mcp__firstbite_local_ci__.list_lanes` still exposes the old 12-lane loaded-host catalog with no `resplit_currency_api`. The reuse command is now runnable and freshness-green, but trust stays red: source `previous-loaded-mcp-artifact:reports/firstbite-loaded-mcp-lanes.json`, repos `resplit_web,resplit_ios,strongyes_web,moussey`, lane count `12`, FX repo missing.
- Fresh proof:
  - live `mcp__firstbite_local_ci__.list_lanes` -> 12 loaded-host lanes, no `resplit_currency_api`.
  - `node --check scripts/capture-loaded-mcp-probe.js` -> pass.
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/capture-loaded-mcp-probe.test.js tests/reliability-cockpit.test.js` -> 44/44.
  - `npm run mcp:loaded-probe -- --reuse-existing` -> rc `0`; status red, freshness green.
  - `npm run mcp:loaded-probe` -> expected rc `1`; error now says to pipe live MCP JSON, pass `--input`, or use `--reuse-existing` without proving host restart.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; cockpit remains `RED - missing required trust contract`; `loaded-mcp-refresh` is runnable with `npm run mcp:loaded-probe -- --reuse-existing`.
  - `npm run trust:preflight` -> expected rc `1`; cockpit remains red with commands `7 green, 2 yellow, 0 red`.
  - `npm test` -> 183/183.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `loaded-mcp-refresh`, `npm run mcp:loaded-probe -- --reuse-existing`, `Use --reuse-existing only to refresh the previous artifact`, and `Loaded MCP Probe`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-loaded-probe-command.png` and `reports/resplit-fx-reliability-cockpit-mobile-loaded-probe-command.png`; both reported no horizontal overflow.
- Current blocker: this removes an operator trap, not a launch blocker. Loaded Codex/Cursor MCP host is still stale/red until restarted/reloaded and recaptured with a live 15-lane catalog; M4 is still support-only until M4-local execute proof exists; source promotion is still unlanded; clean tracked FirstBite proof remains blocked on that bundle; Grafana Tempo/Loki evidence is absent; strict release-history coverage still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: restart/reload the Codex/Cursor MCP host and capture live `list_lanes` with `npm run mcp:loaded-probe -- --input /tmp/firstbite-loaded-mcp.json`, or land the source-promotion bundle and rerun clean `worktree=true` FirstBite proof. Keep the reuse command as freshness maintenance only.

## 2026-05-25 04:04 EDT

- `GO/m4-peer-boundary`, `GO/mcp-catalog-delta`, `GO/proof-freshness-ledger`, `GO/operator-recovery-flow`, `GO/operator-action-queue`, `GO/source-promotion-packet`, `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/m4-peer-execution`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now parses `m4_peer_probe` and `m4_fresh_clone_packet` from the FirstBite operating readout, renders `M4 peer boundary` in the cockpit, adds a `M4 peer execution boundary` trust contract, adds a `m4-peer-execute-proof` operator action, and flags the peer as support-only until an M4-local `run_lanes` execute report exists.
- Current local-agent truth: live `mcp__firstbite_local_ci__.list_lanes` still exposes the old 12-lane loaded host catalog with no `resplit_currency_api`, while the repo-backed FirstBite readout remains `repo-manifest-v2` with 15/15 declared lanes. Fresh operating readout is yellow: `17/18` latest lane proof(s) pass, non-FX `resplit_ios_ui_full` is the failed lane, active checkout manifest readiness is still false, Moussey `/coding` is ready, and M4 is `stale_receiver_manual_local_refresh_required` / support-only. The M4 fresh-clone packet is available at `/Users/leokwan/.agent-ledger/firstbite-m4-fresh-clone-plan/m4-fresh-clone-after-mcp15-20260525/fresh-clone-commands.sh`, but it is a handoff only until run on the M4 Pro.
- Fresh proof:
  - live `mcp__firstbite_local_ci__.list_lanes` -> 12 loaded-host lanes, no `resplit_currency_api`.
  - `bash /Users/leokwan/Development/ai-leo/skills/local-ci/scripts/firstbite-operating-readout.sh` -> fresh readout, `17/18 pass`, `declared_lanes=15/15`.
  - `node scripts/capture-loaded-mcp-probe.js --input reports/firstbite-loaded-mcp-lanes.json` -> refreshed loaded-host artifact age while preserving the stale 12-lane catalog evidence; status red, freshness green. Plain `npm run mcp:loaded-probe` still requires piped MCP JSON input and exits `1` when run without it.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; cockpit remains `RED - missing required trust contract`; `M4 peer execution boundary` is yellow/support-only.
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/reliability-cockpit.test.js tests/capture-loaded-mcp-probe.test.js` -> 43/43.
  - `npm run trust:preflight` -> expected rc `1`; artifact status red with commands `7 green, 2 yellow, 0 red`; cockpit remains red.
  - `npm run test` -> 182/182.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `M4 peer boundary`, `M4 peer is support-only`, `fresh-clone-commands.sh`, and `m4-peer-execute-proof`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-m4-boundary.png` and `reports/resplit-fx-reliability-cockpit-mobile-m4-boundary.png`; both reported no horizontal overflow.
- Current blocker: this improves cross-agent/cross-Mac trust but does not make the system launch-ready. Loaded Codex/Cursor MCP host is still stale/red, M4 is support-only until an M4-local execute report exists, source promotion is still unlanded, clean tracked FirstBite proof is still blocked on that bundle, Grafana Tempo/Loki evidence is still absent, strict release-history coverage still fails for `2026-05-12`..`2026-05-23`, and the broader FirstBite operating readout still has the non-FX `resplit_ios_ui_full` warning.
- Exact next slice: either restart/reload the Codex/Cursor MCP host and recapture loaded `list_lanes`, or run the generated M4 fresh-clone packet on the M4 Pro and capture an M4-local `run_lanes` execute report; keep source promotion, Grafana, and release-history proof as separate gates.

## 2026-05-25 03:54 EDT

- `GO/mcp-catalog-delta`, `GO/proof-freshness-ledger`, `GO/operator-recovery-flow`, `GO/operator-action-queue`, `GO/source-promotion-packet`, `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now builds and renders `localCi.mcpCatalogDelta`, comparing the loaded Codex/Cursor MCP host catalog against the repo-backed FirstBite package catalog. It records missing repos, groups, total lanes, and expected FX lanes so a fresh loaded-host artifact can still be red when the long-lived MCP process is stale.
- Current MCP catalog truth: live `mcp__firstbite_local_ci__.list_lanes` still exposes the old 12-lane host catalog with no `resplit_currency_api` repo or FX lanes. `reports/firstbite-loaded-mcp-lanes.json` is now fresh/green for artifact age but red for trust. Repo-backed package remains green with `repo-manifest-v2`, 15 lanes, and all three `resplit_currency_api_*` lanes. `MCP Catalog Delta` is red: missing `resplit_currency_api`, all three FX lanes, and groups `all_lint` / `resplit_currency_api_all`.
- Fresh proof:
  - live `mcp__firstbite_local_ci__.list_lanes` -> 12 loaded-host lanes, no `resplit_currency_api`.
  - `npm run mcp:loaded-probe` -> refreshed `reports/firstbite-loaded-mcp-lanes.json`; status red, freshness green.
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/reliability-cockpit.test.js tests/capture-loaded-mcp-probe.test.js` -> 43/43.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; proof freshness is `7 fresh, 0 stale/missing`, but loaded-host trust and catalog delta remain red.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `MCP Catalog Delta`, `resplit_currency_api`, and missing FX lane ids; screenshots `reports/resplit-fx-reliability-cockpit-desktop-mcp-delta.png` and `reports/resplit-fx-reliability-cockpit-mobile-mcp-delta.png`; both reported no horizontal overflow.
  - `npm run trust:preflight` -> expected rc `1`; artifact status red with commands `7 green, 2 yellow, 0 red`; cockpit remains `RED - missing required trust contract`.
  - `npm run test` -> 182/182.
- Current blocker: loaded-host evidence is now fresh, but loaded-host trust is still red until Codex/Cursor MCP host is restarted/reloaded and recaptured with all 15 lanes. Source promotion is still unlanded, clean tracked FirstBite proof is still blocked on that bundle, Grafana Tempo/Loki evidence is still absent, strict release-history coverage still fails for `2026-05-12`..`2026-05-23`, and the broader FirstBite operating readout still has the non-FX `resplit_ios_ui_full` warning.
- Exact next slice: restart/reload the Codex/Cursor MCP host and recapture `mcp__firstbite_local_ci__.list_lanes`, or land the source-promotion bundle and rerun clean `worktree=true` FirstBite proof; keep Grafana and release-history proof as separate gates.

## 2026-05-25 03:46 EDT

- `GO/proof-freshness-ledger`, `GO/operator-recovery-flow`, `GO/operator-action-queue`, `GO/source-promotion-packet`, `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now builds and renders `trustModel.evidenceFreshness` as a `Proof Freshness Ledger` directly under the local trust preflight. It names seven proof artifacts (`local-trust-preflight`, `source-promotion-packet`, `selected-firstbite-execute-proof`, `loaded-mcp-host-probe`, `repo-backed-mcp-catalog`, `grafana-otel-smoke`, `firstbite-operating-readout`) and keeps freshness status separate from trust status so a fresh-but-red artifact remains visible as red trust, and a stale loaded-host artifact remains yellow freshness plus red trust.
- Current freshness truth: cockpit verdict remains `RED - missing required trust contract`; `Proof Freshness Ledger` is `yellow` with `6` fresh row(s) and `1` stale/missing row. Fresh-but-red rows are `local-trust-preflight` and `source-promotion-packet`; stale/red row is `loaded-mcp-host-probe`; Grafana smoke is fresh/yellow because Tempo+Loki proof is still missing.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/reliability-cockpit.test.js` -> 39/39.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with `trustModel.evidenceFreshness`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Proof Freshness Ledger`, `local-trust-preflight`, `source-promotion-packet`, `loaded-mcp-host-probe`, and `grafana-otel-smoke`; screenshots `reports/resplit-fx-reliability-cockpit-desktop-proof.png` and `reports/resplit-fx-reliability-cockpit-mobile-proof.png`; both reported no horizontal overflow.
  - `npm run trust:preflight` -> expected rc `1`; artifact status red with commands `7 green, 2 yellow, 0 red`; cockpit remains `RED - missing required trust contract`.
  - `npm run test` -> 181/181.
- Current blocker: this improves operator trust in the evidence surface, but it does not make the system launch-ready. Source promotion is still unlanded, clean tracked FirstBite proof is still blocked on that bundle, the loaded MCP host catalog is still stale/red from the current host artifact, Grafana Tempo/Loki evidence is still absent, strict release-history coverage still fails for `2026-05-12`..`2026-05-23`, and the broader FirstBite operating readout still has the non-FX `resplit_ios_ui_full` warning.
- Exact next slice: refresh the loaded MCP host probe from the actual Codex/Cursor host after restart or land the source-promotion bundle and rerun clean `worktree=true` FirstBite proof; keep Grafana and release-history proof as separate gates.

## 2026-05-25 03:36 EDT

- `GO/operator-recovery-flow`, `GO/operator-action-queue`, `GO/source-promotion-packet`, `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now builds and renders `trustModel.operatorRecoveryFlow`, a derived recovery map above the action queue. It separates runnable local actions from dependency-blocked actions, names the next local command, names the first blocked dependency, and summarizes active boundaries so operators can see source promotion, local CI, loaded MCP, Grafana, data quality, and fleet-readout work without collapsing them into one red state.
- Current recovery-flow truth: status `red`; `3` runnable action(s) now and `3` action(s) waiting on dependencies. Next local action is `source-promotion-review` via `npm run source:promotion-packet`; first blocked action is `clean-firstbite-proof` because the source-promotion bundle must land first.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/reliability-cockpit.test.js` -> 38/38.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with `trustModel.operatorRecoveryFlow`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Operator Recovery Flow`, `Next local action`, `npm run source:promotion-packet`, `First dependency`, `clean-firstbite-proof`, and `external-observability`; screenshots `/tmp/resplit-fx-reliability-cockpit-recovery-flow-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-recovery-flow-mobile-20260525.png`; both reported no horizontal overflow.
  - `npm run trust:preflight` -> expected rc `1`; artifact status red with commands `7 green, 2 yellow, 0 red`; cockpit remains `RED - missing required trust contract`.
  - `npm run test` -> 180/180.
- Current blocker: this improves the operator cockpit, but the system is not launch-trusted. Source promotion is still unlanded, clean tracked FirstBite proof is still blocked on that bundle, the loaded MCP host catalog is still stale, Grafana Tempo/Loki evidence is still absent, strict release-history coverage still fails for `2026-05-12`..`2026-05-23`, and the broader FirstBite operating readout still has the non-FX `resplit_ios_ui_full` warning.
- Exact next slice: either land the source-promotion bundle listed in `reports/resplit-fx-source-promotion-packet.md` and rerun clean `worktree=true` FirstBite proof, or complete the external Grafana/Cloudflare setup and rerun the live OTEL smoke; do not call release readiness green until both are proven.

## 2026-05-25 03:31 EDT

- `GO/operator-action-queue`, `GO/source-promotion-packet`, `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now builds and renders `trustModel.operatorActions`, an ordered operator recovery queue derived from the existing trust contracts instead of a parallel truth source. The queue names the action id, owner, boundary, runnable/dependency state, command, proof requirement, and blocker text for source promotion, clean FirstBite proof, loaded MCP refresh, Grafana OTEL proof, release-history backfill, and the broader FirstBite operating readout.
- UI fix from proof: runnable actions now render blocker evidence in the `Blocker / dependency` column, so the release-history row visibly preserves the exact `May 12-23 history hole` launch blocker instead of hiding it in JSON only.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass.
  - `node --test tests/reliability-cockpit.test.js` -> 37/37.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; `trustModel.operatorActions` has 6 actions: `source-promotion-review`, `clean-firstbite-proof`, `loaded-mcp-refresh`, `grafana-otel-proof`, `release-history-backfill`, and `firstbite-operating-readout`.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Operator Action Queue`, all six action ids, `grafana-otel-proof`, and `May 12-23 history hole`; screenshots `/tmp/resplit-fx-reliability-cockpit-action-queue-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-action-queue-mobile-20260525.png`; both reported no horizontal overflow.
  - `npm run trust:preflight` -> expected rc `1`; artifact status red with commands `7 green, 2 yellow, 0 red`; cockpit remains `RED - missing required trust contract`.
  - `npm run test` -> 179/179.
- Current blocker: this makes the cockpit more operable, but it does not make the system launch-ready. Source promotion is still unlanded, clean tracked FirstBite proof is still blocked on that bundle, the loaded MCP host catalog is still stale, Grafana Tempo/Loki evidence is still absent, strict release-history coverage still fails for `2026-05-12`..`2026-05-23`, and the broader FirstBite operating readout still has the non-FX `resplit_ios_ui_full` warning.
- Exact next slice: use the `Operator Action Queue` top-down: run/review `npm run source:promotion-packet`, land the intended control-plane bundle, rerun the clean `worktree=true` FirstBite proof from tracked source, then refresh the loaded MCP host and live Grafana OTEL smoke.

## 2026-05-25 03:15 EDT

- `GO/source-promotion-packet`, `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: added `scripts/source-promotion-packet.js`, `tests/source-promotion-packet.test.js`, and `npm run source:promotion-packet`. The packet is read-only and derives from the cockpit `Source Promotion Bundle`; it writes `reports/resplit-fx-source-promotion-packet.json` / `.md`, prints the exact `git add -- ...` command for stage candidates, prints an unstage command, and separates hold-by-default dirty files such as `.agents`, snapshot archives, `.gitignore`, queue/docs, and `wrangler.jsonc`.
- Cockpit/preflight integration: `scripts/reliability-cockpit.js` now treats `source:promotion-packet` and its script/test as part of the tracked source contract, and the `Source Promotion Bundle` UI renders `Write packet` / `Review packet` rows. `scripts/trust-preflight.js` now syntax-checks the packet, includes its test in targeted tests, and runs packet generation as expected-yellow evidence while the bundle is still unlanded.
- Fresh proof:
  - `node --check scripts/source-promotion-packet.js` / `scripts/reliability-cockpit.js` / `scripts/trust-preflight.js` -> pass.
  - `node --test tests/source-promotion-packet.test.js tests/reliability-cockpit.test.js tests/trust-preflight.test.js` -> 45/45.
  - `npm run source:promotion-packet` -> expected rc `1`; artifact status red with `19` stage candidates, `24` hold-by-default dirty rows, `6` command drift rows, and cockpit `RED - missing required trust contract`.
  - `npm run test` -> 178/178.
  - `npm run trust:preflight` -> expected rc `1`; artifact status red with commands `7 green, 2 yellow, 0 red`; the two yellow commands are Grafana missing-config proof and source-promotion packet generation.
  - Browser HTML smoke with bundled Playwright -> desktop and mobile render `Source Promotion Bundle`, `npm run source:promotion-packet`, `reports/resplit-fx-source-promotion-packet.md`, and the clean `worktree=true` FirstBite command; screenshots `/tmp/resplit-fx-reliability-cockpit-source-packet-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-source-packet-mobile-20260525.png`; both reported no horizontal overflow.
- Current blocker: this makes source-promotion review safer, but it does not land the bundle and does not make clean-source local CI green. The cockpit remains red: source-promotion is still untracked/unpublished, clean tracked local CI has not been rerun from a landed bundle, the loaded MCP host catalog is still stale, Grafana Tempo/Loki evidence remains absent, strict release-history coverage still fails for `2026-05-12`..`2026-05-23`, and the broader FirstBite operating readout still has a non-FX warning.
- Exact next slice: use `reports/resplit-fx-source-promotion-packet.md` to review and stage only the listed stage candidates, keep hold-by-default rows out unless separately reviewed, then run the clean FirstBite `worktree=true` proof from the landed source and refresh the loaded MCP host catalog.

## 2026-05-25 03:05 EDT

- `GO/otel-proof-checklist`, `GO/firstbite-operating-readout`, `NO-GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/verify-grafana-otel-smoke.js` now writes a token-safe OTEL proof checklist into `reports/grafana-otel-smoke.json`: Worker request trigger, Grafana read configuration, Tempo trace query, and Loki log query. `scripts/reliability-cockpit.js` parses that checklist and renders it as `OTEL Evidence Checklist` inside the cockpit so the Grafana gate names the exact missing proof atoms instead of one opaque yellow summary.
- Fresh proof:
  - `node --check scripts/verify-grafana-otel-smoke.js` -> pass
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/verify-grafana-otel-smoke.test.js tests/reliability-cockpit.test.js` -> 41/41
  - `npm run observability:otel-smoke -- --skip-trigger --output reports/grafana-otel-smoke.json` -> expected rc `2`; artifact stays yellow with checklist rows for skipped trigger, missing `GRAFANA_BASE_URL`, missing `GRAFANA_TEMPO_DATASOURCE_UID`, missing `GRAFANA_LOKI_DATASOURCE_UID`, Tempo blocked, and Loki blocked. The report records only token presence as redacted boolean; no token value is written.
  - `npm run reliability:cockpit` / `npm run trust:preflight` regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` and `reports/resplit-fx-trust-preflight.json` / `.md`; trust preflight remains expected rc `1` with commands `6 green, 1 yellow, 0 red`.
  - `npm run test` -> 174/174.
  - Browser HTML smoke -> desktop and mobile render `Worker request trigger`, `Grafana read configuration`, `Tempo trace query`, and `Loki log query`; screenshots `/tmp/resplit-fx-reliability-cockpit-otel-checklist-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-otel-checklist-mobile-20260525.png`; both reported no horizontal overflow and no token-looking string.
- Current blocker: this makes the telemetry proof path auditable, but it does not make telemetry green. Grafana/Cloudflare external setup is still missing proof: dashboard destinations/token and datasource UIDs must exist, the Worker must be deployed after destinations exist, then `npm run observability:otel-smoke` must run without `--skip-trigger` and return both Tempo and Loki matches. Source promotion, clean tracked local CI, loaded MCP host catalog, strict release-history coverage, and the broader non-FX FirstBite lane warning remain separate blockers.
- Exact next slice: either finish F-B1/F-B2 in `/Users/leokwan/Development/vidux/projects/fleet-otel-observability/PLAN.md` and rerun the live Grafana smoke, or land the Source Promotion Bundle and rerun clean FX FirstBite proof so local-CI trust can move toward green while telemetry waits on external credentials.

## 2026-05-25 02:55 EDT

- `GO/firstbite-operating-readout`, `GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now ingests the latest `~/.agent-ledger/firstbite-operating-readout/*/report.json` and renders a `FirstBite Operating Readout` section plus trust-contract/risk row. This connects the cockpit to the broader local-agent/local-CI operating-room snapshot: repo-backed catalog version, declared lane count, active manifest portability, failed latest lanes, and Moussey `/coding` LAN/local-CI readiness.
- Fresh proof:
  - `bash /Users/leokwan/Development/ai-leo/skills/local-ci/scripts/firstbite-operating-readout.sh` -> wrote `/Users/leokwan/.agent-ledger/firstbite-operating-readout/20260525T064939Z-31520/report.json`; local CI latest proof `17/18 pass`, `1 fail`; declared lanes `15/15`; repo-backed catalog `repo-manifest-v2`; Moussey local verdict `ready`.
  - `npm run reliability:cockpit` / `npm run trust:preflight` regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` and `reports/resplit-fx-trust-preflight.json` / `.md`; cockpit remains `RED - missing required trust contract`.
  - `FirstBite Operating Readout` is yellow: `17/18 lane proof(s) pass`, catalog `repo-manifest-v2` has `15/15` declared lanes, Moussey is ready, `fresh_clone_ready=true`, `active_ready=false`, and the failed lane is non-FX `resplit_ios_ui_full`.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 36/36
  - `npm run test` -> 174/174
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `6 green, 1 yellow, 0 red`; cockpit remains red.
  - Browser HTML smoke -> desktop and mobile render `FirstBite Operating Readout`, `Moussey /coding`, and `resplit_ios_ui_full`; screenshots `/tmp/resplit-fx-reliability-cockpit-operating-readout-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-operating-readout-mobile-20260525.png`; both reported no horizontal overflow.
- Current blocker: this makes fleet/local-agent readiness visible in the FX cockpit, but it does not make FX launch-ready. Clean-source local CI still requires source-promotion and a fresh `worktree=true` FirstBite proof; loaded MCP host catalog remains stale; Grafana Tempo/Loki evidence remains absent; strict release-history validation still fails for `2026-05-12`..`2026-05-23`; and the broader fleet has one non-FX failing latest lane (`resplit_ios_ui_full`).
- Exact next slice: either land the Source Promotion Bundle and rerun clean FX FirstBite proof, or extend the cockpit with a dedicated OTEL/Grafana evidence checklist that names the missing Cloudflare destination/deploy/query proof needed to turn telemetry green.

## 2026-05-25 02:46 EDT

- `GO/source-promotion-bundle`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now renders a `Source Promotion Bundle` section and trust-contract/risk row. It enumerates the exact current-only and modified control-plane paths that make the dirty cockpit/local-CI/preflight capability exist, exposes command drift rows, and prints safe inspect commands plus the clean FirstBite command to run only after the bundle is landed onto tracked source.
- Fresh proof:
  - `npm run reliability:cockpit` / `npm run trust:preflight` regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` and `reports/resplit-fx-trust-preflight.json` / `.md`; cockpit remains `RED - missing required trust contract`.
  - `Source Promotion Bundle` is red: `11 current-only file(s); 6 modified tracked file(s); 0 missing current file(s); 11 file(s) absent from HEAD; 8 file(s) absent from origin/main; 5 command drift row(s)`.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 35/35
  - `npm run test` -> 173/173
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `6 green, 1 yellow, 0 red`; cockpit remains red.
  - Browser HTML smoke -> desktop and mobile render `Source Promotion Bundle`; screenshots `/tmp/resplit-fx-reliability-cockpit-source-promotion-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-source-promotion-mobile-20260525.png`; both reported no horizontal overflow.
- Current blocker: this gives the next agent a precise source-promotion checklist, but clean-source local CI is still not launch proof until those paths and manifest/script command changes are tracked and the clean FirstBite command passes. Loaded MCP host catalog remains stale, Grafana Tempo/Loki evidence remains absent, and strict release-history validation still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: review the `Source Promotion Bundle` paths with `git status --short -- ...`, `git diff -- ...`, and `git ls-files --others --exclude-standard -- ...`; land the intended control-plane bundle onto tracked source; then run the cockpit clean FirstBite command with `worktree=true` and replace the selected stale clean proof.

## 2026-05-25 02:37 EDT

- `GO/clean-proof-readiness-contract`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now renders a `Clean Proof Readiness` section and trust-contract row. It explains the FirstBite runner target contract (`worktree=true` creates disposable detached worktrees from the registered repo `HEAD`; `worktree=false` only proves the active dirty checkout), lists the concrete readiness blockers, and gives both the clean rerun command and the dirty-support command without letting dirty proof count as launch-ready.
- Fresh proof:
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; verdict remains `RED - missing required trust contract`. `Clean Proof Readiness` is red with 5 issues: tracked contract red, selected proof manifest command drift, dirty current-manifest supporting proof, dirty/behind primary checkout, and active checkout portability false.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 34/34
  - `npm run test` -> 172/172
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `6 green, 1 yellow, 0 red`; cockpit remains red.
  - Browser HTML smoke -> desktop and mobile render `Clean Proof Readiness`, `registered repo HEAD`, and the clean `worktree=true` command; screenshots `/tmp/resplit-fx-reliability-cockpit-clean-proof-readiness-20260525.png` and `/tmp/resplit-fx-reliability-cockpit-clean-proof-readiness-mobile-20260525.png`; both reported no horizontal overflow.
- Current blocker: clean-source local CI still cannot honestly go green from the current tracked refs. `HEAD` lacks the repo-local manifest/cockpit files, `origin/main` still has the stale integration command, the loaded Codex MCP host still exposes the old 12-lane catalog, Grafana Tempo/Loki proof is absent, and strict release-history validation still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: land/sync the current `.firstbite/local-ci.json`, package script contract, and cockpit/preflight scripts into tracked source, then run the clean FirstBite command shown in the cockpit so selected proof targets the same contract operators see.

## 2026-05-25 02:26 EDT

- `GO/manifest-drift-visibility`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now compares current `.firstbite/local-ci.json` lane commands against both `HEAD` and `origin/main`, and renders a manifest-lane table inside `Tracked Source Contract`. This closes the false-comfort gap where `origin/main` could contain a manifest file but still carry the wrong command contract.
- Fresh proof:
  - `git fetch --prune` then `git show origin/main:.firstbite/local-ci.json` proved `origin/main` currently declares `resplit_currency_api_integration` as `npm ci && npm run check`, while the active cockpit contract declares `npm ci && npm run check:publish`. `HEAD` in the canonical checkout still has no `.firstbite/local-ci.json`.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; verdict remains `RED - missing required trust contract`. `Tracked Source Contract` now says `3 manifest lane command drift(s)` and the table shows integration current=`check:publish`, HEAD=`missing`, origin/main=`check`.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 33/33
  - Browser HTML smoke -> pass for tracked-source manifest lane table, integration command drift, and red verdict; screenshot `/tmp/resplit-fx-reliability-cockpit-manifest-drift-table-20260525.png`
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `6 green, 1 yellow, 0 red`; cockpit remains red.
  - `npm run test` -> 171/171
- Current blocker: clean-source local CI cannot honestly go green from the current tracked refs. The manifest on `origin/main` is present but stale for integration, the current cockpit/preflight scripts are still not tracked, the loaded Codex MCP host still exposes the old 12-lane catalog, Grafana Tempo/Loki proof is absent, and strict release-history validation still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: land/sync the current cockpit contract and `.firstbite/local-ci.json` command changes into a tracked branch, then rerun FirstBite from clean source so the selected proof can replace the dirty supporting proof.

## 2026-05-25 02:20 EDT

- `GO/current-manifest-proof-visibility`, `NO-GO/clean-tracked-local-ci`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/tracked-source-contract`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now keeps `latestComplete` and `latestCleanComplete` in the MCP proof summary, then renders a `Current Manifest Proof` section when a newer pass matches the current `.firstbite/local-ci.json` commands but is not the selected clean-source proof. This makes the fresh dirty pass visible without letting it override the clean tracked-source gate.
- Fresh proof:
  - Repo-backed FirstBite execute `verify-resplit-fx-current-manifest-execute-20260525` passed all three `resplit_currency_api_*` lanes with the current manifest (`npm ci && npm run check:publish` for integration), but it ran from dirty active source (`dirty 40`, `behind 10`).
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; verdict remains `RED - missing required trust contract`. Local CI now says selected proof `verify-resplit-fx-manifest-origin-main-20260525` still has command drift, while newer current-manifest proof `verify-resplit-fx-current-manifest-execute-20260525` is yellow supporting evidence.
  - Live loaded Codex MCP `mcp__firstbite_local_ci.list_lanes` still exposes the older 12-lane catalog and no `resplit_currency_api`; direct repo-backed package `npm run --silent call -- list_lanes` sees `repo-manifest-v2`, 15 lanes, and the three FX lanes.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 32/32
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `6 green, 1 yellow, 0 red`; cockpit remains red.
  - `npm run test` -> 170/170
  - Browser HTML smoke -> pass for `Current Manifest Proof`, the new run id, red verdict, selected command drift, loaded MCP gap, and dirty-source supporting evidence; screenshot `/tmp/resplit-fx-reliability-cockpit-current-manifest-proof-20260525.png`
- Current blocker: this improves trust visibility, not launch readiness. The clean selected proof still used stale `npm run check`, the fresh current-manifest proof is dirty/behind, the loaded MCP host is stale, tracked-source contract remains red, Grafana Tempo/Loki proof is absent, and strict release-history validation still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: rerun clean-source local CI from a tracked branch using the current `check:publish` manifest, then restart/reload the loaded Codex/Cursor MCP host and recapture a fresh loaded-host probe that sees all 15 lanes.

## 2026-05-25 02:09 EDT

- `GO/loaded-mcp-freshness-contract`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/manifest-proof-command-drift`, `NO-GO/tracked-source-contract`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: added `scripts/capture-loaded-mcp-probe.js` plus `npm run mcp:loaded-probe` so a live Codex/Cursor `mcp__firstbite_local_ci.list_lanes` result can be captured as `reports/firstbite-loaded-mcp-lanes.json` instead of hand-edited. The cockpit now separates loaded-host catalog correctness from probe artifact freshness; old loaded-MCP artifacts turn yellow after 60 minutes even if their lane contents look plausible.
- Fresh proof:
  - Live Codex Desktop `mcp__firstbite_local_ci.list_lanes` still exposed only the older 12-lane catalog (`resplit_web`, `resplit_ios`, `strongyes_web`, `moussey`) and no `resplit_currency_api` repo or FX lanes.
  - `npm run mcp:loaded-probe -- --input reports/firstbite-loaded-mcp-lanes.json ...` -> wrote a fresh loaded-host artifact; probe status stayed red (`repo missing; missing 3/3 expected lane(s)`), freshness green (`0m old`).
  - `node --check scripts/reliability-cockpit.js`, `node --check scripts/capture-loaded-mcp-probe.js`, `node --check scripts/trust-preflight.js` -> pass
  - `node --test tests/capture-loaded-mcp-probe.test.js tests/reliability-cockpit.test.js tests/trust-preflight.test.js` -> 39/39
  - `npm run test` -> 169/169
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; verdict remains `RED - missing required trust contract`
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `6 green, 1 yellow, 0 red`; yellow remains Grafana missing-config proof rc `2`
  - Browser HTML smoke -> pass for `Loaded MCP Probe`, `Freshness`, fresh green probe evidence, red loaded-MCP catalog truth, and red verdict; screenshot `/tmp/resplit-fx-reliability-cockpit-loaded-mcp-fresh-red-20260525.png`
- Current blocker: this improves evidence freshness, not launch readiness. The loaded MCP host still needs a restart/reload before it sees the 15-lane repo-backed catalog and FX lanes; selected local-CI proof still has command drift; tracked source contract is red; Grafana Tempo/Loki proof is absent; strict release-history validation still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: restart/reload the Codex/Cursor MCP host and recapture `npm run mcp:loaded-probe` from the real live `list_lanes` result; then rerun clean-source FX local CI using the current `check:publish` manifest.

## 2026-05-25 01:50 EDT

- `GO/agent-activity-matrix`, `GO/trust-preflight-bundle`, `NO-GO/telemetry-proof`, `NO-GO/loaded-mcp-catalog`, `NO-GO/manifest-proof-command-drift`, `NO-GO/tracked-source-contract`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now builds a normalized `Agent Activity Matrix` from recent repo/shared ledger rows and renders it in the GUI. Rows show status, age, agent family, lane/handoff, proof artifact, and summary so local coding-agent work is inspectable from the cockpit instead of hidden in raw JSONL. The status classifier keeps `in_progress` rows yellow and has a regression guard so the word `unresolved` cannot be mistaken for resolved recovery evidence.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 29/29
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with `Agent Activity Matrix`
  - `npm run trust:preflight` -> expected rc `1`, artifact status red with commands `5 green, 1 yellow, 0 red`; the yellow command is Grafana missing-config proof rc `2`
  - `npm run test` -> 164/164
  - Browser HTML smoke -> pass for `Agent Activity Matrix`, `Local Trust Preflight`, yellow `in_progress` rows, and red verdict; screenshot `/tmp/resplit-fx-reliability-cockpit-agent-matrix-final-20260525.png`
  - Matrix proof column now prefers concrete artifacts such as `reports/resplit-fx-trust-preflight.json`, `reports/grafana-otel-smoke.json`, screenshots, or report JSON paths instead of dumping long command-summary prose.
- Current blocker: cockpit remains red for the same launch-trust reasons: stale loaded Codex/Cursor MCP catalog, latest selected local-CI proof command drift, tracked-source contract red, missing Grafana Tempo/Loki proof, and strict release-history validation gap for `2026-05-12`..`2026-05-23`.
- Exact next slice: reload the loaded MCP host and capture a fresh `list_lanes` artifact, then rerun clean-source FX local CI using the current `check:publish` manifest. In parallel, finish F-B1/F-B2 so `npm run observability:otel-smoke` can attach real Tempo/Loki evidence.

## 2026-05-25 01:42 EDT

- `GO/trust-preflight-bundle`, `NO-GO/telemetry-proof`, `NO-GO/loaded-mcp-catalog`, `NO-GO/manifest-proof-command-drift`, `NO-GO/tracked-source-contract`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: added `scripts/trust-preflight.js` plus `npm run trust:preflight`. The preflight writes `reports/resplit-fx-trust-preflight.json` and `.md`, runs the safe local proof commands, and refreshes the cockpit so the GUI renders a `Local Trust Preflight` section. The command exits nonzero while the cockpit is red, so exit code alone cannot be mistaken for launch readiness.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --check scripts/trust-preflight.js` -> pass
  - `node --test tests/trust-preflight.test.js tests/reliability-cockpit.test.js tests/verify-grafana-otel-smoke.test.js` -> 37/37
  - `npm run test` -> 162/162
  - `npm run trust:preflight -- --full` -> expected rc `1`, artifact status red with commands `8 green, 2 yellow, 0 red`; yellow commands are Grafana missing-config proof rc `2` and strict release validation (`npm run validate:release`) rc `1` (`available 18/30`, missing `2026-05-12`..`2026-05-23`)
  - `npm run validate:release` -> expected fail (`available 18/30`, missing `2026-05-12`..`2026-05-23`)
  - Generated `reports/resplit-fx-trust-preflight.json` / `.md`; cockpit `Local Trust Preflight` shows mode `full`, status red, and the full command bundle
  - Browser HTML smoke -> pass for `Local Trust Preflight`, full test suite, strict release validation, Grafana missing-config proof, trust contracts, and red verdict; screenshot `/tmp/resplit-fx-reliability-cockpit-trust-preflight-20260525.png`
- Current blocker: this makes the local trust proof reproducible, not green. Loaded Codex/Cursor MCP is still stale, the selected clean local-CI proof still has manifest command drift, the local-CI contract is not clean-tracked, Grafana Cloud Tempo/Loki proof is absent, and strict release-history validation still fails for `2026-05-12`..`2026-05-23`.
- Exact next slice: reload the loaded MCP host and capture a fresh `list_lanes` artifact, then rerun clean-source FX local CI with the current `check:publish` manifest. In parallel, F-B1/F-B2 still need Grafana/Cloudflare destination proof before `npm run observability:otel-smoke` can turn green.

## 2026-05-25 01:31 EDT

- `GO/grafana-otel-verifier`, `NO-GO/telemetry-proof`, `NO-GO/loaded-mcp-catalog`, `NO-GO/manifest-proof-command-drift`, `NO-GO/tracked-source-contract`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: added `scripts/verify-grafana-otel-smoke.js` plus `npm run observability:otel-smoke`, so the repo now has a read-only operator path to create the exact JSON artifact the cockpit requires for Grafana Tempo + Loki proof. The cockpit recognizes this combined verifier alongside the older Tempo-only verifier and still refuses to turn telemetry green without a fresh matching artifact.
- Follow-up delta: the cockpit now emits and renders a `Trust Contracts` table that names each launch gate, current truth, proof artifact, and exact next action. This does not downgrade any blocker; it makes the red/yellow contracts operator-readable from the first screen.
- Fresh proof:
  - `node --check scripts/verify-grafana-otel-smoke.js` -> pass
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/verify-grafana-otel-smoke.test.js tests/reliability-cockpit.test.js` -> 32/32
  - `npm run test` -> 157/157
  - `npm run observability:otel-smoke -- --skip-trigger --output reports/grafana-otel-smoke.json` -> expected rc `2`, yellow artifact with Tempo/Loki missing and no Grafana token in report
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`; verdict remains `RED - missing required trust contract`; `Trust Contracts` row `Release-history strict coverage` now cites the stricter `npm run validate:release` failure (`available 18/30`, missing `2026-05-12`..`2026-05-23`)
  - Playwright HTML smoke -> pass for red verdict, `Trust Contracts`, loaded MCP next action, OTEL/Grafana next action, and strict release-history row; screenshot `/tmp/resplit-fx-reliability-cockpit-trust-contracts-20260525.png`
  - `npm run check:publish` -> pass with known recovery warnings for missing `2026-05-12`..`2026-05-23`
  - `npm run smoke:deploy` -> pass (`date=2026-05-25`, `historyPoints=30`, Cloudflare Pages latest OK)
  - `npm run validate:release` -> expected fail (`available 18/30`, missing `2026-05-12`..`2026-05-23`)
- Current blocker: this creates a stronger cockpit handoff, not launch-green proof. Loaded Codex/Cursor MCP is still stale, the selected clean local-CI proof has manifest command drift, the local-CI contract is not clean-tracked, Grafana Cloud Tempo/Loki proof is absent, and strict release-history validation still fails.
- Exact next slice: reload the loaded MCP host, rerun clean-source local CI with the current `check:publish` contract, run `npm run observability:otel-smoke` with Grafana read-only service-account env after F-B1/F-B2 are wired, and keep release readiness yellow until `npm run validate:release` passes.

## 2026-05-25 01:12 EDT

- `GO/repo-backed-mcp-catalog-visibility`, `NO-GO/loaded-mcp-catalog`, `NO-GO/manifest-proof-command-drift`, `NO-GO/tracked-source-contract`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now runs the repo-backed FirstBite MCP `list_lanes` probe during `npm run reliability:cockpit` and renders it as a separate `Repo-Backed MCP Catalog` section. This separates control-plane truth (`repo-manifest-v2`, 15 declared lanes, FX lanes present) from the loaded Codex/Cursor host probe (`12` lanes, missing `resplit_currency_api`), so a stale host can no longer be confused with a stale package.
- Fresh proof:
  - Repo-backed package probe: `/Users/leokwan/Development/ai-leo/skills/resplit-watch/mcp/firstbite-local-ci` reports `repo-manifest-v2`, `lane_count=15`, `declared_count=15`, `resplit_currency_api_*` present, `fresh_clone_ready=true`, `ready=false`.
  - Loaded Codex MCP probe remains stale: `reports/firstbite-loaded-mcp-lanes.json` has `12` lanes, no `resplit_currency_api`, and cockpit keeps `Loaded MCP lane catalog gap` red.
  - FirstBite operating readout `20260525T051051Z-48735` reports `local_ci=15/15 pass`, `declared_lanes=15/15`, repo-backed MCP catalog `repo-manifest-v2`; summary: `/Users/leokwan/.agent-ledger/firstbite-operating-readout/20260525T051051Z-48735/summary.md`.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 25/25
  - `npm run test` -> 150/150
  - `npm run check:publish` -> pass with explicit recovery warnings for the known `2026-05-12`..`2026-05-23` history gap
  - `npm run smoke:deploy` -> pass (`date=2026-05-25`, `historyPoints=30`, Cloudflare Pages latest OK)
  - `npm run validate:release` -> expected fail (`available 18/30`, missing `2026-05-12`..`2026-05-23`)
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with verdict `RED - missing required trust contract`
  - Browser smoke of regenerated cockpit -> pass; screenshot `/tmp/resplit-fx-reliability-cockpit-repo-backed-vs-loaded-mcp-20260525.png`
- Current blocker: this improves observability and diagnosis but not launch truth. The loaded MCP host still needs restart/reload proof, the selected clean branch MCP proof still has manifest command drift, the active checkout is dirty/behind with current-only contract files, Grafana Tempo/Loki proof is absent, and strict release-history readiness remains yellow.
- Exact next slice: restart/reload the Codex/Cursor MCP host or capture a fresh loaded-host `list_lanes` artifact that sees all 15 lanes, then produce clean-source FX proof whose lane commands match the current manifest (`check:publish`) before treating local CI as green.

## 2026-05-25 00:59 EDT

- `NO-GO/manifest-proof-command-drift`, `GO/ledger-risk-honesty`, `NO-GO/tracked-source-contract`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now requires latest MCP proof lane commands to match the current repo manifest commands before local CI can go green. It also stops promoting repaired or in-progress ledger rows into a duplicate top-level `Agent ledger failure history` risk; repaired failure rows remain visible in the Agent State section.
- Fresh proof:
  - A parallel clean branch proof appeared: `verify-resplit-fx-manifest-origin-main-20260525` ran from `/Users/leokwan/Development/resplit-currency-api-worktrees/local-ci-manifest` on branch `codex/local-ci-fx-manifest`, source `f97ecfa98a7c`, dirty `0`, ahead `1`, behind `0`, and passed all three lanes.
  - That proof is not accepted as current-manifest green because its integration command was `npm ci && npm run check`, while the canonical dirty checkout manifest currently declares `npm ci && npm run check:publish`.
  - Regenerated cockpit local-CI status is red: `Latest MCP proof command drift: resplit_currency_api_integration expected "npm ci && npm run check:publish", ran "npm ci && npm run check"`.
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 23/23
  - `npm run test` -> 148/148
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with verdict `RED - missing required trust contract`
  - Browser smoke of regenerated cockpit -> pass; screenshot `/tmp/resplit-fx-reliability-cockpit-command-drift-red-20260525.png`
  - Ledger handoff emitted with `handoff_status=in_progress`; `ledger-fleet-health --repo resplit-currency-api --archive` -> healthy, and `audit_ledger_quality.sh` -> 0 failures / 0 warnings
- Current blocker: local CI now has a promising clean branch proof, but the canonical checkout, manifest, and current package scripts are still split. The loaded Codex/Cursor MCP host remains stale at the old catalog, no Grafana Tempo/Loki proof artifact exists, and strict release-history governance remains yellow until the May 12-23 hole is settled.
- Exact next slice: align the canonical `.firstbite/local-ci.json` command contract with the clean branch proof or rerun a clean branch proof using the current `check:publish` manifest, then reload the loaded MCP host and attach Grafana Tempo/Loki evidence.

## 2026-05-25 00:49 EDT

- `NO-GO/tracked-source-contract`, `GO/execution-source-honesty`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now renders a `Tracked Source Contract` section and JSON contract summary. It compares the repo-owned local-CI manifest, required package scripts, and script file paths across the dirty current checkout, `HEAD`, and `origin/main`, then promotes missing/untracked contract source to a red trust risk. Tests cover an untracked `.firstbite/local-ci.json` plus current-only `check:publish`/cockpit scripts.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 21/21
  - `npm run test` -> 146/146
  - `npm run --silent lint` in the FirstBite MCP package -> pass
  - Clean-worktree MCP aggregate `verify-resplit-fx-clean-worktree-contract-4-20260525` -> `overall=fail`: unit passes after `npm run generate`, UI/live Worker smoke passes, integration remains non-green because clean tracked `HEAD` cannot run the current `check:publish` contract.
  - Execution worktrees remain clean (`dirty_count=0`) but detached from stale local `HEAD=16f7d4e63769`, `origin/main=6267a98b1c70`, behind 9, `sync_status=not_origin_main`. Primary checkout during proof is dirty and behind (`dirty_count=34`, behind 9).
  - The cockpit still selects clean red run `verify-resplit-fx-clean-worktree-contract-4-20260525` ahead of newer dirty pass `restore-resplit-fx-active-contract-20260525`, which remains yellow history.
  - Tracked-source contract is red: `.firstbite/local-ci.json` and `scripts/reliability-cockpit.js` are missing from `HEAD`/`origin/main`; `check:publish` and `reliability:cockpit` are current-only package scripts; `package.json`, `currscript.js`, `scripts/smoke-check-deploy.js`, and `scripts/validate-package.js` are modified current-source inputs.
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with verdict `RED - missing required trust contract`
  - Playwright loopback cockpit smoke -> pass for red verdict, `Tracked Source Contract`, `.firstbite/local-ci.json`, `check:publish`, selected clean-worktree run 4, dirty active-source history row, loaded MCP gap, and Grafana gap; screenshot `/tmp/resplit-fx-reliability-cockpit-tracked-source-red-20260525.png`
- Current blocker: the cockpit is doing the right thing and staying red. The repo-owned lane declaration and the package/check/cockpit scripts are not all tracked on the execution source; the loaded Codex/Cursor MCP host still exposes the stale 12-lane catalog; no Grafana Tempo/Loki proof artifact exists; strict release-history governance remains yellow until the May 12-23 hole is settled.
- Exact next slice: land or sync `.firstbite/local-ci.json`, `scripts/reliability-cockpit.js`, `check:publish`, `reliability:cockpit`, and clean generated-package expectations onto the tracked branch/current `origin/main`; rerun clean-worktree local CI from current source; restart/reload the host MCP process; attach real Grafana Tempo/Loki proof. Keep the cockpit red until those converge.

## 2026-05-25 00:32 EDT

- `NO-GO/clean-worktree-proof`, `GO/execution-source-honesty`, `NO-GO/loaded-mcp-catalog`, `NO-GO/telemetry-proof`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: the FirstBite local-CI MCP now records `primary_source_state` separately from `execution_source_state`, keeps compatibility `source_state` as execution truth, records lane `worktree`, `cwd`, and `primary_repo_path`, and preserves non-zero command reasons. The cockpit now renders `Execution sync`, `Execution dirty / ahead / behind`, `Primary checkout during proof`, lane worktree status, and generic non-UI lane errors. Tests cover dirty-primary/clean-execution proof and generic lane diagnostics.
- Fresh proof:
  - Direct repo-backed MCP clean-worktree aggregate `verify-resplit-fx-clean-worktree-contract-3-20260525` -> `overall=fail`: unit and integration are non-green, UI/live Worker smoke passes.
  - Execution worktrees are clean (`dirty_count=0`) but detached from stale local `HEAD=16f7d4e63769`, `origin/main=6267a98b1c70`, behind 9, `sync_status=not_origin_main`.
  - Primary checkout during the run is dirty and behind (`dirty_count=34`, behind 9), which is now shown separately instead of being confused with execution source state.
  - A newer dirty active-source aggregate `restore-resplit-fx-active-contract-20260525` passed with `worktree=false`, `dirty_count=35`, and behind 9. The cockpit now keeps that row in history as yellow and does not let it mask the clean-worktree red proof.
  - UI smoke proof is current: `smoke-check-deploy: OK (date=2026-05-25, historyPoints=30, cf=https://resplit-currency-api.pages.dev)`.
  - Unit lane stops on clean HEAD because `tests/validate-package.test.js` expects a generated `package/` tree that is not present in the tracked worktree.
  - Integration lane exits code 1. Clean HEAD `package.json` has no `check:publish`, while the dirty working tree does; this confirms the local-CI contract is still relying on unlanded source.
  - `node --test tests/reliability-cockpit.test.js` -> 20/20
  - `npm run test` -> 145/145
  - `npm run --silent lint` in the FirstBite MCP package -> pass
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with verdict `RED - missing required trust contract`
  - Playwright loopback cockpit smoke -> pass for red verdict, selected clean-worktree run id, dirty active-source history row, execution/primary source split, worktree column, and loaded MCP gap; screenshot `/tmp/resplit-fx-reliability-cockpit-clean-worktree-red-20260525.png`
- Current blocker: this is better honesty, not launch-green proof. `.firstbite/local-ci.json`, `scripts/reliability-cockpit.js`, `check:publish`, and clean generated-package expectations are not all tracked on the execution source. The loaded Codex/Cursor MCP host remains stale at the 12-lane catalog, no Grafana Tempo/Loki proof artifact exists, and strict release-history governance remains yellow until the May 12-23 hole is settled.
- Exact next slice: land or sync the repo-owned local-CI manifest/scripts/package expectations onto clean tracked source, rerun the clean-worktree aggregate from current `origin/main`, restart/reload the loaded MCP host, and attach a real Grafana Tempo/Loki evidence artifact. Keep the cockpit red until those proof boundaries converge.

## 2026-05-25 00:18 EDT

- `NO-GO/loaded-mcp-catalog`, `GO/control-surface-honesty`, `NO-GO/release-readiness`, `NO-GO/telemetry-proof` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now consumes a loaded-host `list_lanes` probe artifact at `reports/firstbite-loaded-mcp-lanes.json`, compares it to the repo-owned `.firstbite/local-ci.json` contract, renders a `Loaded MCP Probe` section, and promotes stale Codex/Cursor MCP catalogs to a first-class red trust gap instead of relying only on nurse-log wording. `tests/reliability-cockpit.test.js` covers stale MCP tool payloads, current repo-manifest catalogs, and trust-gap suppression of the older generic stale-server row.
- Fresh proof:
  - In-process Codex Desktop MCP `mcp__firstbite_local_ci__.list_lanes` -> 12 lanes only, no `resplit_currency_api`, no FX lane group
  - Repo-backed MCP package `npm run --silent call -- list_lanes '{}'` -> `catalog_version=repo-manifest-v2`, 15 lanes including `resplit_currency_api_unit`, `resplit_currency_api_integration`, and `resplit_currency_api_ui`
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 18/18
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with verdict `RED - missing required trust contract`
- Current blocker: strict release-history governance remains yellow until the May 12-23 history hole is backfilled or ages out. Separate blockers remain unchanged: the loaded Codex/Cursor MCP host process is stale and must restart or reload before in-app `firstbite-local-ci` can be trusted for FX lanes, source proof is still dirty/behind (`HEAD=16f7d4e63769`, `origin/main=6267a98b1c70`, behind 9, dirty count 34), and no Grafana Tempo/Loki proof artifact exists.
- Exact next slice: restart/reload the host MCP process and capture a green loaded-MCP probe, then run clean tracked-source local-CI proof and attach a real Grafana Tempo/Loki artifact. Until the host process is reloaded, use the repo-backed package `npm run call` path as the canonical MCP truth.

## 2026-05-25 00:08 EDT

- `GO/agent-ledger-recovery-visibility`, `GO/control-surface-honesty`, `NO-GO/release-readiness`, `NO-GO/telemetry-proof` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now reads ledger proof and handoff status, computes 24h agent-ledger health, tracks recovered failure history plus append-only repair markers, and renders `Ledger health`, `Latest failure`, and `Latest recovery` in the cockpit. `tests/reliability-cockpit.test.js` covers the repair-marker case. Append-only ledger repair markers were emitted for stale red event `evt_bd57d02d`; no ledger history was rewritten.
- Fresh proof:
  - `bash ~/Development/ai-leo/skills/ledger/scripts/audit_ledger_quality.sh` -> 0 failures / 0 warnings
  - `~/Development/ai/hooks/ledger-fleet-health.sh --repo resplit-currency-api --archive` -> healthy, `failures:0`, `resolved_failures:4`
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 15/15
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json`
  - `npm run test` -> 140/140
  - Playwright loopback cockpit smoke -> pass for yellow verdict, ledger health, latest recovery, agent-ledger trust gap, and existing Grafana/source/release gaps; screenshot `/tmp/resplit-fx-reliability-cockpit-ledger-health-20260525.png`
- Current blocker: source proof is still dirty/behind (`HEAD=16f7d4e63769`, `origin/main=6267a98b1c70`, behind 9, dirty count 34), no Grafana Tempo/Loki proof artifact exists, strict release-history governance remains yellow until the May 12-23 history hole is backfilled or ages out, and the loaded Desktop MCP server still needs reload/restart before it exposes the FX repo lanes.
- Exact next slice: keep the cockpit yellow, but use it as the trust readout. Next move is clean tracked-source MCP proof plus a real Grafana Tempo/Loki evidence artifact; separately reload the Desktop MCP process so the in-app `firstbite-local-ci` server sees the FX lanes.

## 2026-05-24 23:56 EDT

- `GO/live-freshness-recovered`, `GO/control-surface-honesty`, `NO-GO/release-readiness`, `NO-GO/telemetry-proof` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now preserves exact live-smoke stale-surface diagnostics for both Cloudflare and GitHub fallback failures, so the next failure identifies which public surface lagged. `tests/reliability-cockpit.test.js` covers the GitHub fallback error path.
- Fresh proof:
  - Wall clock: `2026-05-25T03:54:22Z`
  - Live endpoints recovered to `2026-05-25`: Cloudflare latest, GitHub Pages fallback latest, both archive manifests, and Worker coverage (`availableDays=30`, `missingDayCount=0`, no signals)
  - `npm run smoke:deploy` -> pass: `smoke-check-deploy: OK (date=2026-05-25, historyPoints=30, cf=https://resplit-currency-api.pages.dev)`
  - Direct updated MCP package run `verify-resplit-fx-mcp-execute-after-gh-pages-catchup-20260525` -> `overall=pass`: unit, integration, and UI/live smoke all green
  - Desktop-loaded `firstbite-local-ci` MCP server is still stale and does not expose `resplit_currency_api`; direct package path remains the current canonical proof until the MCP process reloads
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 15/15
  - `npm run reliability:cockpit` -> regenerated report with verdict `YELLOW - control surface exists, proof still split`; latest complete proof is `verify-resplit-fx-mcp-execute-after-gh-pages-catchup-20260525`
- Current blocker: no longer public FX freshness. The remaining yellow/red trust boundaries are dirty/behind checkout proof (`HEAD=16f7d4e63769`, `origin/main=6267a98b1c70`, behind 9, dirty count 34), no Grafana Tempo/Loki proof artifact, strict release-history governance still yellow until the May 12-23 history hole is backfilled or ages out, and the loaded Desktop MCP server needs reload/restart before it exposes the FX repo lanes.
- Exact next slice: keep cockpit yellow until proof runs from clean tracked source and a real Grafana Tempo/Loki artifact lands. Treat the delayed 03:50/03:51 UTC workflow recovery plus GitHub Pages `max-age=600` fallback lag as a schedule/propagation risk, not an active live-freshness failure.

## 2026-05-24 23:48 EDT

- `NO-GO/post-grace-live-freshness`, `GO/control-surface-honesty`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now renders MCP proof history, keeps newer partial reruns separate from complete manifest proof, preserves red/yellow/green trust status per proof, and parses failed live-smoke logs down to the exact date mismatch. The cockpit also names the stale loaded Desktop MCP server as its own trust gap when the nurse log carries that boundary.
- Fresh proof:
  - Post-grace wall clock: `2026-05-25T03:46:07Z`, after the explicit publish grace deadline `2026-05-25T03:45:00Z`
  - `npm run smoke:deploy` -> fails hard: `cloudflare latest date expected 2026-05-25, got 2026-05-24`
  - `EXPECTED_DATE=$(date -u +%F) npm run smoke:deploy` -> fails with the same mismatch
  - Direct updated MCP package run `verify-resplit-fx-mcp-execute-after-grace-20260525` -> `overall=fail`: unit pass, integration pass, UI/live Worker smoke fail
  - `node --test tests/reliability-cockpit.test.js` -> 15/15
  - `npm run test` -> 140/140
  - `npx wrangler deploy --dry-run --env production --outdir /tmp/resplit-fx-wrangler-dry-run` -> passed locally without deploy
  - `npm run reliability:cockpit` -> regenerated report with verdict `RED - missing required trust contract`; latest complete proof is `verify-resplit-fx-mcp-execute-after-grace-20260525`
  - Playwright loopback HTML smoke -> pass for red verdict, after-grace run id, exact mismatch, proof history, and OTEL/Grafana gap; screenshot `/tmp/resplit-fx-reliability-cockpit-after-grace-red.png`
- Current blocker: production/latest FX is still serving `2026-05-24` after the 03:45 UTC grace window. The report is correctly red even though unit/integration lanes pass, because the user-facing UI/live smoke lane failed. Separate yellow gaps remain: dirty/behind checkout, no Grafana Tempo/Loki proof artifact, release-history strict readiness, and stale Desktop MCP server visibility.
- Exact next slice: find why no 2026-05-25 publish/deploy has landed, restore the daily publish path, then rerun `npm run smoke:deploy` and direct MCP aggregate until the latest complete proof is green without publish grace. Do not upgrade telemetry until a real Grafana Tempo/Loki artifact exists.

## 2026-05-24 23:14 EDT

- `GO/publish-grace-contract`, `GO/complete-mcp-proof-selection`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/smoke-check-deploy.js` now models a narrow post-publish grace window instead of turning the local UI lane hard-red during the 00:00/03:00 UTC publish handoff. Explicit `EXPECTED_DATE` remains strict for the GitHub Actions deploy smoke; only default local smoke can accept the prior date during grace and it prints a warning. `.firstbite/local-ci.json` now lets the smoke script own that contract. `scripts/reliability-cockpit.js` now parses live-smoke diagnostics only from the UI lane and refuses to let a newer partial MCP rerun eclipse the freshest complete unit+integration+UI proof.
- Fresh proof:
  - Live probe at `2026-05-25T03:08Z`: Cloudflare Pages `latest/usd.json` and `meta.json` still served `2026-05-24`; Worker `/health` returned 200 with release `4bfb27d979c1`; Worker `/coverage?anchorDate=2026-05-25` used prior-day fallback with `quoteResolvedLagDays=1`.
  - `EXPECTED_DATE=2026-05-25 npm run smoke:deploy` -> still fails strictly: `cloudflare latest date expected 2026-05-25, got 2026-05-24`
  - `npm run smoke:deploy` -> passes with warning: `publish window grace accepted 2026-05-24; strict expected 2026-05-25 until 2026-05-25T03:45:00.000Z`
  - `node --test tests/smoke-check-deploy.test.js tests/reliability-cockpit.test.js` -> 27/27
  - Direct updated MCP package run `verify-resplit-fx-mcp-execute-publish-grace-20260525` -> `overall=pass` for unit, integration, and UI; UI lane carries the publish-grace warning
  - `npm run test` -> 140/140
  - `npx wrangler deploy --dry-run --env production --outdir /tmp/resplit-fx-wrangler-dry-run` -> passed locally without deploy
  - `npm run reliability:cockpit` -> regenerated cockpit with verdict `YELLOW - control surface exists, proof still split`; local-CI is yellow because the latest complete proof passed inside publish grace
  - Playwright static HTML smoke -> pass, screenshot `/tmp/resplit-fx-reliability-cockpit.png`
- Current blocker: this is not green launch truth. The public FX date is still `2026-05-24` during the 03:00 UTC publish grace, the proof still ran on dirty checkout `HEAD=16f7d4e63769` 8 commits behind `origin/main`, Grafana Tempo/Loki evidence is still missing, strict release-history readiness remains yellow for the May 12-23 history hole, and the loaded Desktop MCP server still needs reload/restart to see the FX repo.
- Exact next slice: after `03:45Z`, rerun `npm run smoke:deploy` and the direct MCP aggregate. If Cloudflare is still on `2026-05-24`, the same contract should flip back to hard red; if it catches up, the cockpit should remain yellow only for dirty/behind, Grafana evidence, and release-history gaps.

## 2026-05-24 23:04 EDT

- `GO/trust-cockpit-contract`, `NO-GO/local-ci-latest`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `scripts/reliability-cockpit.js` now treats the latest FirstBite MCP execute report as a freshness/source-state contract, not just a lane status blob. The HTML/JSON cockpit renders report age, MCP `created_at`, mode, source sync state, dirty/ahead/behind counts, lane source heads, lane logs, and a Grafana Tempo/Loki proof artifact contract. `inspectTelemetry` can only go green when Workers observability config exists and a fresh JSON proof shows both Tempo and Loki matches.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 12/12
  - `npm run test` -> 134/134
  - `npx wrangler deploy --dry-run --env production --outdir /tmp/resplit-fx-wrangler-dry-run` -> passed locally without deploy
  - Desktop-loaded `firstbite-local-ci` MCP `list_lanes` is stale and still does not expose `resplit_currency_api`
  - Direct updated MCP package run `verify-resplit-fx-mcp-execute-trust-contract-20260525` -> `overall=fail`: unit pass, integration pass, UI/live Worker smoke fail
  - UI/live Worker smoke failure reason: `cloudflare latest date expected 2026-05-25, got 2026-05-24`
  - `npm run reliability:cockpit` -> regenerated `reports/resplit-fx-reliability-cockpit.html` / `.json` with verdict `RED - missing required trust contract`
  - Playwright static HTML smoke -> pass, screenshot `/tmp/resplit-fx-reliability-cockpit.png`
- Current blocker: latest trusted local-CI aggregate proof is red because the public Worker/CDN smoke is stale against the 2026-05-25 UTC expectation. The proof still ran on dirty checkout `HEAD=16f7d4e63769`, 8 commits behind `origin/main`; Grafana Tempo/Loki evidence is missing; release-history readiness remains yellow for the May 12-23 history hole; the already-loaded Desktop MCP server needs reload/restart to see the new FX repo.
- Exact next slice: decide whether the FX UI lane should keep failing stale live publish at the 03:00 UTC boundary or model an explicit grace/current-live contract, then rerun the direct MCP package after the public deployment/date catches up. Do not claim telemetry green until a real Grafana proof artifact exists under `reports/`, `docs/`, or `.cursor/proofs/`.

## 2026-05-24 22:47 EDT

- `GO/local-ci-control-plane`, `GO/config-staged`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: `wrangler.jsonc` now declares Cloudflare first-party Workers observability for logs and traces with `grafana-logs-prod` and `grafana-traces-prod` destinations; production sampling is `0.1`. The cockpit parser now recognizes Cloudflare's current `destinations: [...]` config shape, renders destination names in `reports/resplit-fx-reliability-cockpit.html`, and only flags stale Grafana INBOX rows when old Worker-side OTEL package/secret instructions remain.
- Fresh proof:
  - Cloudflare docs rechecked 2026-05-25 UTC: Grafana export uses Cloudflare dashboard destination names and `observability.logs.destinations` / `observability.traces.destinations`
  - `node --check scripts/reliability-cockpit.js` -> pass
  - `node --test tests/reliability-cockpit.test.js` -> 8/8
  - `npm run test` -> 130/130
  - `npx wrangler --version` -> `4.75.0`
  - `npx wrangler deploy --dry-run --env production --outdir /tmp/resplit-fx-wrangler-dry-run` -> passed locally without deploy; Worker bundle built and exited at dry-run
  - `npm run call -- run_lanes '{"mode":"execute","group":"resplit_currency_api_all","worktree":false,"run_id":"verify-resplit-fx-mcp-execute-prod-scope-20260525"}'` in the updated FirstBite MCP package -> `overall=pass` for all 3 FX lanes
  - `npm run reliability:cockpit` -> regenerated the HTML with verdict `YELLOW - control surface exists, proof still split`, local-CI green, telemetry yellow, and no stale Grafana row
- Current blocker: this is still not production telemetry proof. Cloudflare/Grafana dashboard destinations and a real deployed request trace/log in Tempo/Loki are absent, and strict release-history readiness remains yellow for the May 12-23 history hole. Local-CI proof is still dirty-checkout proof on `HEAD=16f7d4e63769`, 8 commits behind `origin/main`. The already-loaded Desktop MCP tool instance also needs a reload/restart before it exposes the new `resplit_currency_api_all` group; the updated package direct-call path is green.
- Exact next slice: turn this into clean tracked proof, then complete fleet OTEL `F-C1` by deploying after dashboard destinations exist and attaching a Grafana Tempo/Loki evidence artifact.

## 2026-05-24 22:41 EDT

- `GO/local-ci-control-plane`, `NO-GO/release-readiness` for the Resplit FX reliability cockpit.
- Shipped delta in working tree: the FirstBite local-CI MCP registry now recognizes `resplit_currency_api`, exposes `resplit_currency_api_all`, and includes the FX unit lane in `critical_fast`. The cockpit now normalizes MCP `pass` proof to green and renders the latest MCP proof packet directly in `reports/resplit-fx-reliability-cockpit.html`.
- Fresh proof:
  - `npm run lint` in `/Users/leokwan/Development/ai-leo/skills/resplit-watch/mcp/firstbite-local-ci` -> `node --check` passed for server and helper scripts
  - `npm run call -- list_lanes '{}'` in the MCP package -> listed `resplit_currency_api` plus `resplit_currency_api_unit`, `resplit_currency_api_integration`, and `resplit_currency_api_ui`
  - `npm run call -- run_lanes '{"mode":"dry_run","group":"resplit_currency_api_all","worktree":false,"run_id":"verify-resplit-fx-mcp-dry-run-20260525"}'` -> planned all 3 FX lanes with manifest/source-state metadata
  - `npm run call -- run_lanes '{"mode":"execute","group":"resplit_currency_api_all","worktree":false,"run_id":"verify-resplit-fx-mcp-execute-20260525"}'` -> `overall=pass` for unit, integration, and UI lanes
  - `npm run call -- status '{"limit":5}'` -> `latest_lane_proof` includes all 3 `resplit_currency_api_*` lanes as `pass` with source head `16f7d4e63769`
  - `node --check scripts/reliability-cockpit.js && node --test tests/reliability-cockpit.test.js` -> 7/7
  - `npm run reliability:cockpit` -> regenerated the HTML with `Latest MCP Proof` for run `verify-resplit-fx-mcp-execute-20260525`
- Current blocker: local-CI proof is now green only for the dirty primary checkout (`worktree=false`, `sync_status=dirty`, local `HEAD` 8 commits behind `origin/main`). Overall cockpit verdict remains red because current `wrangler.jsonc` still lacks the Cloudflare observability block, the Grafana/Tempo proof is absent, and strict release-history readiness is still blocked on the May 12-23 window.
- Exact next slice: turn this dirty-checkout proof into tracked/clean-main proof, then complete the fleet OTEL `F-C1` Cloudflare/Grafana adoption path so telemetry can turn green without leaning on the old April nurse-log claim.

## 2026-05-24 22:30 EDT

- `GO/control-plane-adapter`, `NO-GO/release-readiness` for the Resplit FX launch reliability surface.
- Shipped delta in working tree: `.firstbite/local-ci.json` declares repo-owned `resplit_currency_api` unit, integration, and user-facing smoke lanes. `npm run reliability:cockpit` now generates a read-only internal cockpit at `reports/resplit-fx-reliability-cockpit.html` / `.json`, joining local-CI lane contracts, git/source state, package gates, nurse-log and INBOX truth, agent ledger state, FirstBite MCP proof discovery, and OTEL/Grafana readiness.
- Fresh proof:
  - `node --check scripts/reliability-cockpit.js`
  - `node --test tests/reliability-cockpit.test.js` -> 7/7
  - `npm run reliability:cockpit` -> wrote `/Users/leokwan/Development/resplit-currency-api/reports/resplit-fx-reliability-cockpit.html`
  - `npm run test` -> 129/129
  - `EXPECTED_DATE=2026-05-24 npm run smoke:deploy` -> `smoke-check-deploy: OK (date=2026-05-24, historyPoints=30, cf=https://resplit-currency-api.pages.dev)`
  - `bash ~/Development/ai-leo/skills/ledger/scripts/audit_ledger_quality.sh` -> 0 failures / 0 warnings
  - `~/Development/ai/hooks/ledger-fleet-health.sh --repo resplit-currency-api --archive` -> healthy
  - central ledger claim emitted for the cockpit-owned files on lane `resplit-fx-reliability-cockpit` (`evt_464f741c`)
- Current blocker: the cockpit is deliberately red/yellow, not green. Current `wrangler.jsonc` has no Cloudflare observability block, no matching FirstBite MCP execute proof was found for `resplit_currency_api`, and strict release-history readiness is still blocked on the May 12-23 window.
- Exact next slice: add `resplit_currency_api` to the FirstBite MCP repo registry or equivalent manifest discovery path, then run all three declared lanes for source-state proof; after that, complete the fleet OTEL `F-C1` adoption path and Grafana/Tempo smoke so the cockpit can turn telemetry green for real.

## 2026-05-24 04:34 EDT

- `GO/publish-recovery`, `NO-GO/release-readiness` for `resplit-currency-api` daily publish recovery on `main`.
- Shipped delta in working tree: `currscript.js` now writes `history/30d` from the actual latest 30-calendar-day window instead of the last 30 available snapshots. `validate-package` now has explicit gates: default publish validation warns on incomplete calendar coverage/archive debt, while `STRICT_HISTORY_COVERAGE=1` / `npm run check` fails release readiness until all 30 calendar days are present. `smoke-check-deploy` defaults to current UTC date, checks Cloudflare and GitHub fallback freshness, fails stale production, and warns through known archive-only recovery coverage signals only when quote freshness is exact.
- Fresh proof:
  - `node --test tests/smoke-check-deploy.test.js tests/validate-package.test.js` -> 14/14
  - `npm run check:publish` -> generated May 24 package, warned on `history/30d calendar coverage incomplete: available 18/30` and archive debt, then 116/116 tests passed
  - `npm run check` -> intentionally fails at `validate:release`: missing 2026-05-12..2026-05-23 from the latest 30-calendar-day history window
  - `gh -R firstbitelabsllc/resplit-currency-api run list --workflow 'Update Currency Rates' --limit 5` -> run `26356178180` succeeded on head `3e16f54`
  - `curl https://resplit-currency-api.pages.dev/latest/usd.json` -> `date=2026-05-24`
  - `curl https://fx.resplit.app/coverage?from=AED&to=USD&anchorDate=2026-05-24&days=30` -> exact/fresh quote, `availableDays=18/30`, `missingDayCount=12`, `signals=["history_range_incomplete","archive_gap_detected"]`
  - `npm run smoke:deploy` -> passes on date `2026-05-24` with explicit worker coverage recovery warning (`missingDayCount=12`, `mismatchCount=24`)
- Exact next slice: backfill May 12-23 from an authoritative historical source or keep strict release validation red until the missing dates age out.
- Current blocker: full 30-calendar-day FX history is not restored. FX freshness is recovered, but release-history readiness remains yellow and `RESPLIT-WEB-2` should stay open until strict coverage is restored or intentionally accepted.

## 2026-04-22 20:27 EDT

- `GO/current` for `resplit-currency-api` observability on branch `vidux/grafana-worker-otel`.
- Shipped delta: closed the live Grafana blocker by setting Wrangler production secret parity for `OTEL_EXPORTER_OTLP_ENDPOINT` and `OTEL_EXPORTER_OTLP_HEADERS`, then redeployed the Worker from the OTel lane.
- Fresh proof:
  - `npx wrangler secret list --env production` now includes `OTEL_EXPORTER_OTLP_ENDPOINT` and `OTEL_EXPORTER_OTLP_HEADERS`
  - `npx wrangler deploy --env production` -> version `a38ab2ac-4f55-4eef-bc3d-fd2f57bf9254`
  - `node scripts/smoke-check-deploy.js` -> `OK`
  - `node scripts/verify-grafana-tempo.mjs --base-url https://fx.resplit.app --since 30m --timeout-ms 90000` -> matched trace `f709c30953b26adbf0cd36484097837` with `service.name=resplit-currency-api-worker`, `service.version=0910674b67881687ace961d35f7b785cf53f14b1`, verification span `resplit.fx.coverage.verify.<request-id>`, `authSource=OTEL_EXPORTER_OTLP_HEADERS`, and `endpointSource=OTEL_EXPORTER_OTLP_ENDPOINT`
- Exact next slice: keep `resplit-currency-api` on fast-exit unless Tempo goes dark again; observability focus shifts back to StrongYes production merge/deploy and any downstream `resplit-ios` proof that consumes FX telemetry.
- Current blocker: none on the Worker lane; the remaining observability gap now lives outside this repo (StrongYes production path, dashboards/alerts, optional `resplit-ios` consumer proof).

## 2026-04-10 23:25 EDT

- `NO-GO` overall launch (resplit-ios trunk dirty with active claude thread mid-fix on `PendingScanRecord` SwiftData @Model bricked-app failure); `GO/current` for `resplit-currency-api`.
- Shipped delta: nurse log refresh only — no product/runtime code delta this cycle. Repo remained quiet for 7 days since the 2026-04-03 entry. Test count grew from `72 → 74` (two additive tests landed between 2026-04-03 and 2026-04-10, surfaced via `npm run check`).
- Fresh proof:
  - `npm run check` -> `74/74` tests green (was `72/72` in last entry).
  - `npm run smoke:deploy` -> `OK (date=2026-04-11, historyPoints=30, cf=https://resplit-currency-api.pages.dev)` — FX worker has rolled forward to the `2026-04-11` publish date already (correct for late evening Eastern, since the daily publish runs on UTC and we're past 03:00 UTC).
- Known / unknown / forgotten work surfaced:
  - known: external launch blocker is now resplit-ios `PendingScanRecord` SwiftData `@Model` bricked-app failure (TestFlight build 1795 was rejected, fastlane has been failing for 24+ hours, commits-pending climbed to 574 in the deploy ledger). An active claude thread is mid-fix; the sidecar fleet (bug-fixer, launch-loop, ios-ux, currency) is in coordinated standby per the standing directive.
  - unknown: the 2 new tests added between the 2026-04-03 entry and now — worth a quick `git log -- tests/` audit if a deeper slice runs later. Recent commits show `08091af vidux: harden fx publish freshness window` and `6898cdf feat: surface FX freshness diagnostics` — likely the source of the new test coverage.
  - forgotten: still no active hot-file owner in this repo and ledger queries still return newline-only — `.agent-ledger/activity.jsonl` for this repo remains unstreamed.
- Exact next slice: keep `resplit-currency-api` on fast-exit unless (a) FX freshness drifts past the publish window, OR (b) a launch-critical FX bug surfaces in resplit-ios manual expense / multi-currency split paths, OR (c) the resplit-ios trunk wall clears and the launch train resumes.
- Current build boundary: trunk `origin/main` `9ff5e74` (was `afdaf5ca`); FX publish date `2026-04-11`; worker 30-day coverage green.
- Latency: `hygiene` `1m`, `discovery` `2m`, `implementation` `0m` (read-only verify), `proof/wait` `2m`.

<promise>SKIP: external blocker</promise>

## 2026-04-22 19:09 EDT

- `GO/current` for `resplit-currency-api` observability on branch `vidux/grafana-worker-otel`.
- Shipped delta: added an opt-in `/coverage` verification span and a repo-local Tempo verifier CLI (`scripts/verify-grafana-tempo.mjs`) on top of the existing Worker OTel bootstrap, then wired the exact proof steps into `README.md`, `RUNBOOK.md`, `INFRASTRUCTURE.md`, and `docs/grafana-otel-smoke-2026-04-22.md`.
- Fresh proof:
  - `npm run check`
  - `npm run smoke:deploy` -> `smoke-check-deploy: OK (date=2026-04-22, historyPoints=30, cf=https://resplit-currency-api.pages.dev)`
  - `node --test tests/fx-worker-otel.test.js tests/fx-tempo-verifier.test.js` -> `10/10` green on the verifier path
  - `npm run observability:tempo-smoke -- --base-url http://127.0.0.1:8787` hit local `/coverage` with request id `210a0718-5cbd-4435-b3cd-e8853787cf87`, but Grafana returned `No Tempo traces found for service resplit-currency-api-worker within 90000ms`
  - `node scripts/verify-grafana-tempo.mjs --base-url https://fx.resplit.app --skip-hit --since 24h --timeout-ms 1000` also returned `No Tempo traces found for service resplit-currency-api-worker within 1000ms`
- Exact next slice: verify Wrangler OTLP secrets on the deployed Worker, redeploy, and rerun `npm run observability:tempo-smoke -- --base-url https://fx.resplit.app` until `service.name=resplit-currency-api-worker` appears in Tempo.
- Current blocker: the deterministic verifier works, but the Worker runtime still is not exporting any traces to Grafana Cloud.

## 2026-04-03 18:43 EDT

- `NO-GO` overall launch; `GO/current` for `resplit-currency-api`.
- Shipped delta: docs-only checkpoint from fresh disposable lane `/Users/leokwan/Development/resplit-currency-api-worktrees/codex/vidux-20260403-183332-fx-fast-exit` on branch `codex/vidux-20260403-183332-fx-fast-exit`; no product/runtime code delta.
- Fresh proof:
  - `PATH=/opt/homebrew/bin:$PATH npm ci`
  - `PATH=/opt/homebrew/bin:$PATH npm run check` -> `72/72` tests green; publish artifacts regenerated for `2026-04-03` with clean git state.
  - `PATH=/opt/homebrew/bin:$PATH npm run smoke:deploy` -> `OK (date=2026-04-03, historyPoints=30, cf=https://resplit-currency-api.pages.dev)`.
  - Runbook probes with explicit user agent confirm live parity on `2026-04-03`: Cloudflare latest `date=2026-04-03`; history `points=30`; archive manifest `latestDate=2026-04-03`; GitHub Pages fallback latest `date=2026-04-03`; worker quote `resolutionKind=exact` + `resolvedDate=2026-04-03`; worker coverage `historyCoverage.availableDays=30`, `missingDayCount=0`, `archiveGapCount=0`.
  - GitHub Actions CLI probe confirms latest runs `23931026119` (`pages build and deployment`) and `23930995489` (`Update Currency Rates`) are `completed/success`.
- Known / unknown / forgotten work surfaced:
  - known: external launch blocker unchanged — `resplit-ios` Task 9 manual/TestFlight verification on build `876`, plus unresolved claimed row `ADm7xviYCN62zYBS8O6FZ4c` in `/Users/leokwan/Development/resplit-ios/.cursor/plans/app-store-feedback.plan.md`.
  - unknown: `.agent-ledger/activity.jsonl` in this repo remains newline-only and `ledger --gc --report` is unavailable (`ledger command not found`).
  - forgotten: confirmed no active hot-file owner in this repo and preserved attached-root hygiene (all execution from fresh `origin/main` worktree, no local dirt promoted as checkpoint).
- Exact next slice: keep `resplit-currency-api` on fast-exit unless workflow/runbook truth goes red; continue shipper pressure on `resplit-ios` build-`876` manual/TestFlight wall.
- Current build boundary: trunk `origin/main` `afdaf5ca`; FX publish date `2026-04-03`; worker 30-day coverage green.
- Latency: `hygiene` `8m`, `discovery` `17m`, `implementation` `5m`, `proof/wait` `10m`.

<promise>SKIP: external blocker</promise>
